#pragma once

#include <util/stream/output.h>

namespace NAntiRobot {
    enum EClientType {
        CLIENT_GENERAL = 0,
        CLIENT_XML_PARTNER,
        CLIENT_AJAX,
        CLIENT_CAPTCHA_API,
    };
}
