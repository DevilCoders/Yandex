Набор вспомогательных утилит для kernel/segmentator, kernel/dater и kernel/segnumerator.
Более подробная справочная информация для всех утилит доступна по --help.

archives_demo - пример приложения, распаковывающего сегменты из текстового архива.

viewers - различные вьюеры
    time_t_2_date - утилита, конвертирующая форматы дат (TODO: обобщить на форматы TDaterDate).
    dater_server *) - датировщик документов. 
            На вход принимает список url документов, на выходе - список url TAB дата. 
            Принимает данные как из stdin, так и в теле post по http. Поддерживает много настроек.

    segviewer *) **) - веб-вьювер для сегментатора и датировщика.
    
tests - тесты на сегментатор и датировщики
    dater_test *) - функциональный тест для старого датировщика.
    dater2_test *) - функциональный тест для нового датировщика.
    segmentator_test *) - функциональный тест для сегментатора.

quality_measurers - утилиты для измерения качества
    dater_quality *) - утилита для оценки качества датирования документа.
    links_quality *) - утилита для оценки качества выделения линковых блоков в документе.
    mains_features *) - утилита для генерирования входного файла matrixnet для обучения сегментатора.
    mains_quality *) - утилита для оценки качества выделения главного контента и его заголовка.
 
*) для работы этой утилиты требуются файлы dict.dict (arcadia_tests_data/recognize/dict.dict), 
    htparser.ini (arcadia/yweb/common/roboconf/htparser.ini) и 2ld.list (arcadia/yweb/urlrules/2ld.list), 
    лежащие под своими именами в одной директории 

**) может работать как с документами, раздаваемыми по http, так и локальными (file://).
