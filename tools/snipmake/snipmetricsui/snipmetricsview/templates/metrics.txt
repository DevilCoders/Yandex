{% for snip, vals in metricsBySnip.items %}{{ snip.query.text }}	{{ snip.url }}	{{ snip.title }}	{{ snip.snippet }}	{% for val in vals %}{{ val.metric.shortName }}:{{ val.value }} {% endfor %}
{% endfor %}