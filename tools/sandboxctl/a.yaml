---
service: infracloudnetwork
title: nvgpumanager ci
ci:
  secret: sec-01eb1ev312nt4461fyqx248tet
  runtime:
    sandbox-owner: RCCS-ADMINS
  triggers:
    - on: pr
      flow: build_tool
  flows:
    build_tool:
      title: build binary
      jobs:
        buildpkg:
          title: build sandboxctl binary
          task: common/arcadia/build_arcadia_project_for_all
          input:
            build_subtasks_created: true
            build_system: ya
            platforms_list:
              - linux
              - win32
              - darwin
            project: tools/sandboxctl/bin/sandboxctl
            sources_package_subtasks_created: true
            strip_binaries: true
