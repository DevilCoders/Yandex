updates: [status] # Обновляем только статус тикета, не пишем коммент, и не обновляем никакие поля
statuses:
  # Пока не вмёржили - держим статус inReview
  not_started: inReview
  in_progress: inReview
  blocked:     inReview
  completed:   inReview
  # Вмёржили - значит resolved
  merged:      resolved
