service: autocheck
title: Запуск Large и Native тестов

ci:
  secret: sec-123
  runtime:
    sandbox-owner: SOME-OWNER

  flows:
    run-flow:
      jobs:
        execute-single-task:
          title: ${flow-vars.title}
          task: dummy
