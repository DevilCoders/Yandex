service: autocheck
title: Запуск Large и Native тестов

ci:
  secret: sec-345
  runtime:
    sandbox-owner: SOME-OTHER-OWNER

  flows:
    run-flow:
      jobs:
        execute-single-task:
          title: ${flow-vars.title}
          task: dummy
