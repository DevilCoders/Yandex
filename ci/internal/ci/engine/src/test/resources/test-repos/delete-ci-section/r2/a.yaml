service: ci
title: R1

ci:
  secret: sec-xxx
  runtime:
    sandbox-owner: CI

  actions:
    dummy-action:
      flow: dummy-flow

  flows:
    dummy-flow:
      jobs:
        single-task:
          task: dummy
