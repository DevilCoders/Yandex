service: ci
title: large-autostart, native-builds
ci:
  secret: sec-01dy7t26dyht1bj4w3yn94fsa
  runtime:
    sandbox-owner: CI

  autocheck:
    large-autostart: [ 'ci' ]
    native-builds:
      default-win-x86_64-debug:
        - arc/local
        - solomon/sysmon
