package ru.yandex.ci.flow.engine.source_code.model;

public class ProducedResource extends AbstractResource {

    public ProducedResource(ResourceObject resource, boolean isList) {
        super(resource, isList);
    }
}
