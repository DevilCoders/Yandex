service: ci
title: invalid yaml
ci:
  flows:
    sawmill:
      title: Woodcutter
      description: sawmill flow
      jobs:
        job1:
          title: job1
          task: example/path/job3
          stage: one
        job2:2
        title: job
        needs: unexisting-job
        task: example/path/job3
        stage: two
