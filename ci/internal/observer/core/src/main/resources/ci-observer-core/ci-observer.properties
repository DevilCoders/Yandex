# YDB
observer.observerYdbConfig.ydbToken=${ci.observer.ydb.oauth-token}
observer.observerYdbConfig.maxPoolSize=1000

# Cache
observer.observerChecksCache.size=4000
observer.iterationsGroupingCache.size=8000
observer.tasksGroupingCache.size=40000
observer.tracesCache.size=100000

observer.observerCache.maxNumberOfWrites=10000
