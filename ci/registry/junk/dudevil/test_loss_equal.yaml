title: Test Workflow loss equals
description: Run Sandbox which runs a nirvana graph
maintainers: dudevil

sandbox-task:
  name: WORKFLOW_CHECK_LOSS_EQUAL
  required-parameters:
    - base_workflow
    - nirvana_token
    - block_code
    - block_output
    - local_params_update

parameters: 
  workflow_ttl: 3600
  workflow_name: "Testing revision ${context.launch_pull_request_info.vcs_info.feature_revision_hash}"
  
versions: 
  stable: 2407962977
