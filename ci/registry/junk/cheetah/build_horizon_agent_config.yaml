title: Build Horizon agent config
description: Build Horizon agent config
maintainers: home

sandbox-task:
  name: BUILD_HORIZON_AGENT_CONFIG
  required-parameters:
    - checkout_arcadia_from_url
    - vertical

parameters:
  name: ${flow-vars.resource-type}
  vertical: ${flow-vars.vertical}
  checkout_arcadia_from_url: "arcadia:/arc/tags/apphost/conf/morda/stable-${tasks.build-morda-graphs-version.result_output | [?path == 'version'].string | [0] | [0]}-1/arcadia"
