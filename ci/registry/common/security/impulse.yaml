title: imPulse Security
description: Запускает сканеры безопасности, доступные в imPulse (http://wiki/security/imPulse)
maintainers: imp

sandbox-task:
  name: ARCADIA_SAST
  required-parameters:
    - analysers
    - checkout_arcadia_from_url
    - organization_id
    - project_id
    - secret
    - target

parameters:
  checkout_arcadia_from_url: "arcadia-arc:/#${context.target_revision.hash}"
  secret: "${context.secret_uid}"
