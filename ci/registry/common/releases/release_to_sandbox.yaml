title: Release to sandbox
description: Press release button on sandbox task
maintainers: release_machine
sources: /release_machine/tasklets/release_to_sandbox
auto-rollback-mode: EXECUTE

tasklet:
  implementation: ReleaseToSandbox

versions:
  stable: 2316242819

resources:
  config:
    common_release_data:
      release_notes: "Release: ${context.title}, url: ${context.ci_url}"
