service: advmachine
title: AdvMachine CI Stuff

arcanum:
  auto_merge:
    enabled: true
    requirements:
      - system: arcanum
        type: comment_issues_closed
      - system: arcanum
        type: st_issue_linked
      - system: ci
        type: build
      - system: ci
        type: tests

  review:
    rules:
      - subpaths: >
          **
        reviewers:
          - name: devops
            ship: 1
            assign: 1
