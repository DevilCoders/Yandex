title: Build AdvMachine
maintainers: advmachine

sandbox-task:
  name: BUILD_ADV_MACHINE_2

parameters:
  ArcadiaUrl: arcadia-arc:/#${context.target_revision.hash}
  YtTokenVault: adv_machine_yt_token
  BuildSystem: semi_distbuild
  nanny_labels:
    - default
  resources:
    - indexer_bins
    - runtime_bins
    - config
    - indexer_config
