title: Upload
description: Upload repository history to Arc VCS
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_UPLOAD
  required-parameters:
    - auth
    - store

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

runtime:
  sandbox:
    kill-timeout: 8h

versions:
  stable: 3290794949
