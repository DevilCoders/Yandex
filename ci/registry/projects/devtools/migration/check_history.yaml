title: Check history
description: Check repository history
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_CHECK_HISTORY
  required-parameters:
    - auth
    - repository

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
