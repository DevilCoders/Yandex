title: Prepare usermap
description: Extract authors and committers, guess logins and publish the result
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_PREPARE_USERMAP
  required-parameters:
    - auth
    - repository

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
