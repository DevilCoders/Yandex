title: Integrate
description: Integrate repository head from uploaded history into Arcadia trunk
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_INTEGRATE
  required-parameters:
    - auth
    - source_id
    - branch
    - arcadia_path

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
