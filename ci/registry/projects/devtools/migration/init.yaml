title: Init
description: Check migration flow input data
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_INIT
  required-parameters:
    - auth
    - ticket
    - in_repo_type
    - in_repo_url
    - in_arcadia_path

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
