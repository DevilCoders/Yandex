title: Acquire
description: Clone repository and fixate it for further processing
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_ACQUIRE
  required-parameters:
    - auth
    - repo_type
    - repo_url
    - source_id

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
