title: Read-only
description: Switch source to read-only state
maintainers: repo

sandbox-task:
  name: BITBUCKET_READONLY_RESTRICTIONS
  required-parameters:
    - auth
    - source_id

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
