title: Check head
description: Check repository main branch head
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_CHECK_HEAD
  required-parameters:
    - auth
    - repository
    - branch

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
