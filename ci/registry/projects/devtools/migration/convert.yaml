title: Convert
description: Transform and convert repository to Arc format
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_CONVERT
  required-parameters:
    - auth
    - repository
    - usermap
    - arcadia_path

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

runtime:
  sandbox:
    kill-timeout: 8h

versions:
  stable: 3290794949
