title: Acquire usermap
description: Load and check mapping of authors and committers into logins
maintainers: repo

sandbox-task:
  name: VCS_MIGRATION_ACQUIRE_USERMAP
  required-parameters:
    - auth
    - repository

parameters:
  binary_executor_release_type: stable

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL

versions:
  stable: 3290794949
