title: Parse Junit report
description: Parse JUnit report and publish the comment to Pull Request
maintainers: devtools
sources: /devtools/ya/tasklets/parse_junit

tasklet:
  runtime: sandbox
  implementation: ParseJunit

versions:
  stable: 3335999137

parameters:
  config:
    path_to_report: "junit_report.xml"

attempts: 1
