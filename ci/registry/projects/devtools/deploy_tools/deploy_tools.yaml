title: DeployTools
description: Creating Pull Request from built ya tools
maintainers: devtools
sources: /devtools/ya/tasklets/deploy_tools/

tasklet:
  runtime: sandbox
  implementation: DeployTools

versions:
  stable: 3369951618

parameters:
  config:
    is_release: true

attempts: 1
