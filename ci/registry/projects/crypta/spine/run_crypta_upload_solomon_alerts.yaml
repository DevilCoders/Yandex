title: Run crypta upload solomon alerts
description: Testenv job
maintainers: g:crypta

sandbox-task:
  name: CRYPTA_UPLOAD_SOLOMON_ALERTS_TASK

parameters:
  environment: stable
  kill_timeout: 6300

requirements:
  sandbox:
    platform: linux
    priority:
      class: SERVICE
      subclass: LOW

runtime:
  sandbox:
    notifications:
      - transport: email
        recipients:
          - crypta-ci
        statuses:
          - FAILURE
          - EXCEPTION
          - TIMEOUT
