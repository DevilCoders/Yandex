title: Build package
description: Build Trencher package
maintainers: velocityandstability

sandbox-task:
  name: TRENCHER_BUILD
  required-parameters:
    - commit
    - flow_launch_id

parameters:
  commit: "${context.target_revision.hash}"
  flow_launch_id: "${context.job_instance_id.flow_launch_id}"
