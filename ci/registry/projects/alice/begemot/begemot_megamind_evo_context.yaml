title: BegemotMegamindEvoContext
description: Run sandbox BEGEMOT_MEGAMIND_EVO_CONTEXT
maintainers: megabegemot

sandbox-task:
  name: BEGEMOT_MEGAMIND_EVO_CONTEXT

parameters:
  evo_in_branch_num: ${context.version_info.major}
  evo_in_tag_num: ${not_null(context.version_info.minor, `0`)}
