title: RunGitLabPipeline
description: Run fintech GitLab pipeline
maintainers: fintech_yacloud_cicd
sources: /fintech/ci/tasklets/run_gitlab_pipeline
auto-rollback-mode: EXECUTE

tasklet:
  runtime: sandbox
  implementation: RunGitLabPipeline

versions:
  stable: 2832210517

parameters:
  config:
    gitlab:
      api_url: https://gitlab-api-proxy.npe.yandex-bank.net/api/v4
      polling_interval: 30

attempts: 1

requirements:
  sandbox:
    client_tags: MULTISLOT # save planet
