title: Build Weather OS
description: Building Porto Layer 2 from CI
maintainers: weatherfront

sandbox-task:
  name: BUILD_PORTO_LAYER_2

parameters:
  use_arc_fuse: true

  arc_revision: trunk
  arc_token: sec-01cqc28nkbabqnxf71me1p6xt1#ARCANUM_TOKEN
  svn_revision: trunk

  layer_name: weather-render-base
  script_env:
    NODEJS_VERSION: ${not_null(flow-vars.nodeVersion, '16.16.0')}
  output_resource_attr:
    # platform: linux_ubuntu_18.04_bionic
    nodejs: ${not_null(flow-vars.nodeVersion, '16.16.0')}
    ttl: inf

requirements:
  sandbox:
    client_tags: PORTOD
    priority:
      class: SERVICE
      subclass: HIGH