title: Build dispatch config resource
description: Build dispatch config resource
maintainers: ydo

sandbox-task:
  name: KOSHER_YA_MAKE

parameters:
  build_type: release
  checkout_arcadia_from_url: "arcadia-arc:/#${context.target_revision.hash}"
  use_aapi_fuse: true
  use_arc_instead_of_aapi: true
  arc_secret: "${context.secret_uid}#ci.token"
  targets: ydo/dispatch/configs
  arts_source: ydo/dispatch/configs/dispatch_config.json
  result_rt: YDO_DISPATCH_CONFIG
  result_single_file: true

requirements:
  sandbox:
    client_tags: "GENERIC & LINUX"
