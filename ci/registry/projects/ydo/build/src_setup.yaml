title: Build src setup
description: Build src setup via ya make
maintainers: ydo

sandbox-task:
  name: BUILD_APP_HOST_SRC_SETUP

parameters:
  build_type: release
  choose_vertical: YDO
  checkout_arcadia_from_url: "arcadia-arc:/#${context.target_revision.hash}"
  breakpad_symbols_dumper_resource_id: 61581470
  arc_token_owner: YDO

requirements:
  sandbox:
    client_tags: "GENERIC & LINUX"

