title: Build geosearch data
description: Build MIDDLESEARCH_GEO_DATA resource for geometasearch
maintainers: geosearch2

sandbox-task:
  name: BUILD_GEOSEARCH_DATA
  required-parameters:
    - checkout_arcadia_from_url
  badges-configs:
    - id: build_report
      module: SANDBOX

parameters:
  checkout_arcadia_from_url: arcadia-arc:/#${context.target_revision.hash}
