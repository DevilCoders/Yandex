title: Проверка ya style
description: Запускает проверку ya style в режиме dry-run с падением при непустом diff
maintainers: yandex-pay

sandbox-task:
  name: FINSRV_PYTHON_PRECOMMIT_CHECK
  required-parameters:
    - project_path
    - project_import_path
    - commit_hash

parameters:
  commit_hash: "${context.target_revision.hash}"
  check_ya_style: true
