title: Deploy Web (trunk)
description: Deploy web (unstable)
maintainers: mt

sandbox-task:
  name: RELEASE_TRANSLATE_WEB
  required-parameters:
    - commit
    - description
    - use_s3_production_bucket
    - use_external_version

parameters:
  commit: "${context.target_revision.hash}"
  description: "Release trunk, commit ${context.target_revision.hash}"
  use_s3_production_bucket: false
  use_external_version: false
