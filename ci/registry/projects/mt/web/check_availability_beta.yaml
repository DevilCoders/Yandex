title: Check availability (beta)
description: Check availability for beta service
maintainers: mt

sandbox-task:
  name: TRANSLATE_AVAILABILITY_TASK
  required-parameters:
    - commit
    - description
    - manifest_url
    - check_manifest
    - host

parameters:
  commit: "${context.target_revision.hash}"
  description: "Check availability for beta on commit ${context.target_revision.hash}"
  manifest_url: "https://release-beta.dev.translate.yandex.ru/yandex-manifest.json"
  check_manifest: true
  host: "beta"
