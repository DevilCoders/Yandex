title: Create release ticket
description: Create release ticket for mt web release
maintainers: mt

sandbox-task:
  name: RELEASE_TRANSLATE_TICKET_TASK
  required-parameters:
    - version
    - description
    - commit
    - assignee
    - followers
    - start_commit
    - end_commit
    - dev

parameters:
  commit: "${context.target_revision.hash}"
  description: "Create release ticket 21.${context.version_info.full}"
  version: "${context.version_info.full}"
  assignee: "${context.flow_triggered_by}"
  followers:
    - acvetkov
    - lenawest
    - kssamoylenko
    - dvorkanton
    - alex-radn
    - ewgeny
    - dyrovigor
    - evg-bogdanov
    - mashashma
  start_commit: "${context.release_vsc_info.previous_release_revision.hash}"
  end_commit: "${context.target_revision.hash}"
  dev: false
