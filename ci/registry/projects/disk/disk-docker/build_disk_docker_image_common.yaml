title: Build disk docker image common
description: Testenv job
maintainers: g:disk-admin
sandbox-task:
  name: BUILD_DISK_DOCKER_IMAGE
parameters:
  environment: production
  docker_image_path: disk/common/common_all
  revision: ${context.target_revision.number}
