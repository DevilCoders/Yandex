title: Build disk docker image clusters
description: Testenv job
maintainers: g:disk-admin
sandbox-task:
  name: BUILD_DISK_DOCKER_IMAGES
parameters:
  environment: production
  commit_paths: '{commit_paths}'
  revision: ${context.target_revision.number}
