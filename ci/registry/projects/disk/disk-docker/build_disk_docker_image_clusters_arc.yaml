title: Build disk docker image clusters
description: Testenv job
maintainers: g:disk-admin
sandbox-task:
  name: BUILD_DISK_DOCKER_IMAGES_ARC
parameters:
  environment: production
  commit_paths: ${tasks.collect_paths.result_output[?path == 'commit_paths'].string[] | join('', @)}
  revision: ${context.target_revision.number}
