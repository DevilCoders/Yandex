title: SUP RELEASE PROJECTS CONFIG
description: релиз конфига проектов
maintainers: sup

sandbox-task:
  name: SUP_AUTO_RELEASE_PROJECTS_CONFIG

parameters:
  rev: "${context.target_revision.hash}"
  use_aapi_fuse: true
  use_arc_instead_of_aapi: true
  check_only: true
