title: SUP CI
description: Run sup/push tests
maintainers: sup
sandbox-task:
  name: PUSH_BUILD
parameters:
  mvn_cmd: '-Dspring.profiles.active=tests,yt -Dtest=!PushGuideDoc'
  arc: "${context.target_revision.hash}"
