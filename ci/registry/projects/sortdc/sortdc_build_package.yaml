title: SortDC build package
description: SortDC build job
maintainers: marakasov
sandbox-task:
  name: BUILD_SORTDC_PACKAGE
parameters:
  environment: production
  build_system: ya
  notify_if_finished: ${context.target_commit.author}
  notify_via: email
  notify_if_failed: ${context.target_commit.author}
  checkout_arcadia_from_url: arcadia-arc:/#${context.target_revision.hash}
  build_type: release
requirements:
  sandbox:
    platform: any
  disk: 70GB
