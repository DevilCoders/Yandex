title: Check Precharge After Memory Map
description: 1. Checks whether files are loaded into memory right after they have been mmapped at the start of search component. 2. Checks whether the component reads from disk at first request. Binary must be built with --build=debug.
maintainers: noapache-web

sandbox-task:
  name: CHECK_PRECHARGE_AFTER_MEMORY_MAP
