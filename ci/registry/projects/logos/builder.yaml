title: Ads Logos Builder
description: Logos Builder
maintainers: g:logos

sandbox-task:
  name: ADS_LOGOS_BUILDER

parameters:
  arc_revision: ${context.target_revision.number}
  logos_secret: "${context.secret_uid}#ci.token"

requirements:
  sandbox:
    platform: linux
