title: Build and release resource
description: YA_MAKE2 -> Resource -> Release
maintainers: g:logos

sandbox-task:
  name: LOGOS_BUILD_AND_RELEASE

parameters:
  build_or_select: build
  revision: ${context.target_revision.hash}
  release_resource: false
  select_resource: by_id

requirements:
  sandbox:
    platform: linux
