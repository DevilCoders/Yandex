title: Test maps b2bgeo clang tidy
description: Testenv job
maintainers: g:b2bgeo-dev
sandbox-task:
  name: BBGEO_CLANG_TIDY
parameters:
  environment: production
  revision: ${context.target_revision.hash}
requirements:
  sandbox:
    platform: linux
