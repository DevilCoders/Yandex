title: Build maps b2bgeo solver docker
description: Testenv job
maintainers: g:b2bgeo-dev
sandbox-task:
  name: BBGEO_SOLVER_AUTO_DEPLOY
parameters:
  environment: production
  revision: ${context.target_revision.hash}
requirements:
  sandbox:
    platform: linux
