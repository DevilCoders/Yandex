title: Balancer Pusher
description: Обновление балансеров
maintainers: maps-infra

sandbox-task:
  template: MAPS_CORE_BALANCER_PUSHER_STABLE

parameters:
  arcadia_url: "arcadia-arc:/#r${context.target_revision.number}"
  review_id: "${context.target_revision.pull_request_id}"
