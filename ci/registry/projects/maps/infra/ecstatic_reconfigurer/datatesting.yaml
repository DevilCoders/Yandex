title: Ecstatic Reconfigurer [Datatesting]
description: Реконфигурация ecstatic
maintainers: maps-infra
sources: /maps/infra/ecstatic/sandbox/reconfigurer/task

sandbox-task:
  template: MAPS_CORE_ECSTATIC_RECONFIGURER_CI_DATATESTING
  required-parameters:
    - run_type

parameters:
  use_arcadia: true
  arcadia_url: "arcadia-arc:/#${context.target_revision.hash}"
