title: Maps masstransit transport regions uploader
maintainers: maps-masstransit

sandbox-task:
  name: MAPS_MASSTRANSIT_TRANSPORT_REGIONS

parameters:
  auto_release: testing
  arcadia_url: "arcadia:/arc/trunk/arcadia/maps/masstransit/configs/regions-json@${context.target_revision.number}"

attempts: 3

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL
