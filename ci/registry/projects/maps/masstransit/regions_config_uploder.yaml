title: Maps masstransit transport regions config uploader
description: Testenv job
maintainers: g:maps-masstransit

parameters:
  revision: ${context.target_revision.number}

sandbox-task:
  name: MAPS_MASSTRANSIT_UPDATE_REGIONS_CONFIG

attempts: 3

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL
