title: Maps masstransit predictor blacklist uploader
maintainers: g:maps-masstransit

sandbox-task:
  name: MAPS_MASSTRANSIT_UPLOAD_PREDICTOR_BLACKLIST

parameters:
  revision: ${context.target_revision.number}
  description: "Upload predictor blacklist ${context.target_revision.number}: {commit_message}"

attempts: 3

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL
