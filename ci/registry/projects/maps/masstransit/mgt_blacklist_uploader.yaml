title: Maps masstransit mgt blacklist uploader
maintainers: g:maps-masstransit

sandbox-task:
  name: MAPS_MASSTRANSIT_UPLOAD_MGT_BLACKLIST

parameters:
  blacklist_path: arcadia:/arc/trunk/arcadia/maps/masstransit/configs/mgt_blacklist/blacklist.txt@${context.target_revision.number}
  environment: production
  garden: PRODUCTION

attempts: 3

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL
