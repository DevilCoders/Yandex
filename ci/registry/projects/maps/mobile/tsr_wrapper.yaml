title: Сборка приложения с помощью TSR
maintainers: maps-core-mobile-arcadia-ci

sandbox-task:
  name: MAPS_MOBILE_TSR_WRAPPER
  required-parameters:
    - ssh_key
    - config_path

parameters:
  commit: ${context.target_revision.hash}
  config_from_repository: true
