title: Группировка результатов multiply/by задачи
maintainers: maps-core-mobile-arcadia-ci
sources: maps/mobile/sandbox/group_sandbox_resources

tasklet:
  runtime: sandbox
  implementation: GroupSandboxResources

parameters:
  config:
    tasks_json: ${to_string(tasks)}

versions:
  stable: 2617180497
