title: Task waiting for arcanum review is commited
description: Задача, ожидающая коммита ревьюшки в аркадию
maintainers: maps-infra
sources: /maps/infra/sandbox/wait_for_review

sandbox-task:
  template: MAPS_CORE_WAIT_FOR_REVIEW_STABLE
  required-parameters:
    - review_id

parameters:
  arcanum_token: "${context.secret_uid}#ci.token"
