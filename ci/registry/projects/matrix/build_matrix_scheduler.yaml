title: Build matrix scheduler
description: Build matrix scheduler binary
maintainers: speechkit_ops_alice_notificator

sandbox-task:
  name: BUILD_MATRIX_SCHEDULER

parameters:
  checkout_arcadia_from_url: arcadia-arc:/#${context.target_revision.hash}
