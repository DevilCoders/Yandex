title: DeployDebianPackage
description: Installs a new debian package on antifraud VMs that run on Z2 deploy system
maintainers: ochingopin
sources: /quality/antifraud/tasklets/deploy_debian_package

tasklet:
  runtime: sandbox
  implementation: DeployDebianPackage

versions:
  stable: 3068016344
