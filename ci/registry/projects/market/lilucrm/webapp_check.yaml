title: Запуск CI проврок
description: Запускает CI проверки (eslint, typescript, etc...)
maintainers: loudless
sandbox-task:
  name: MARKET_DEVEXP_FRONT_CI
  required-parameters:
    - arcadia_url
    - command

parameters:
  arcadia_url: "arcadia-arc:/#${context.launch_pull_request_info.vcs_info.feature_branch}"
  app_dir: "market/lilucrm/operator-window/webapp"
