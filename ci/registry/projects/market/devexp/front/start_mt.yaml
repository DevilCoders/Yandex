title: Запуск мультитестинга
description: Запускает создание мультитестинга
maintainers: marketdevexp
sandbox-task:
  name: MARKET_DEVEXP_FRONT_START_MT
  required-parameters:
    - ticket_id
    - arcadia_url
    - tsum_project
    - tsum_pipeline_type
    - review_id

parameters:
  arcadia_url: "arcadia-arc:/#${context.launch_pull_request_info.vcs_info.feature_branch}"
  ticket_id: "${context.launch_pull_request_info.issues[0].id}"
  review_id: "${context.launch_pull_request_info.pull_request.id}"
