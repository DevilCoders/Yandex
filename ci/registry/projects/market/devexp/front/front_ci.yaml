title: Запуск CI проверку
description: Запускает CI проверку (eslint, jest, etc...)
maintainers: marketdevexp
sandbox-task:
  name: MARKET_DEVEXP_FRONT_CI
  required-parameters:
    - arcadia_url
    - command
    - nodejs_version
    - ubuntu_version
    - app_dir

parameters:
  arcadia_url: "arcadia-arc:/#${context.launch_pull_request_info.vcs_info.feature_branch}"
