title: Проверка мультитестинга
description: Проверяет создание мультитестинга
maintainers: marketdevexp
sandbox-task:
  name: MARKET_DEVEXP_FRONT_CHECK_MT
  required-parameters:
    - ticket_id
    - tsum_project
    - review_id

parameters:
  ticket_id: "${context.launch_pull_request_info.issues[0].id}"
  review_id: "${context.launch_pull_request_info.pull_request.id}"
