title: Скачивание шаблона из Firebase Remote Config
description: Скачивает актуальный конфиг и коммитит в файл remoteConfig.json
maintainers: rkendzhaev
sandbox-task:
  name: MARKET_MOBILE_DOWNLOAD_REMOTE_CONFIG_TASK
  required-parameters:
    - project_id
    - key
    - target_path
    - author
