title: Деплой в Няню с помощью дашборда
description: Выполняет деплой в Няню с помощью дашборда
maintainers: bonv
sandbox-task:
  name: DEPLOY_NANNY_DASHBOARD
  required-parameters:
    - deployment_task_id
    - deployment_nanny_dashboard_name
    - deployment_nanny_dashboard_recipe
    - deployment_release_status
    - vault_name
    - vault_owner
parameters:
  deployment_nanny_bool_wait: true
  wait_deployment: 120
  services_from_recipe: true
