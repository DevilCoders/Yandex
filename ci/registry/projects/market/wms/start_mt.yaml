title: Запуск мультитестинга
description: Запускает создание мультитестинга
maintainers: serpentine
sandbox-task:
  name: WMS_START_MT
  required-parameters:
    - ticket_id
    - arcadia_url
    - tsum_project
    - tsum_pipeline_type
    - robot_secret

parameters:
  arcadia_url: "arcadia-arc:/#${context.launch_pull_request_info.vcs_info.feature_branch}"
  ticket_id: "${context.launch_pull_request_info.issues[0].id}"
  branch: "${context.launch_pull_request_info.vcs_info.feature_branch}"
  tsum_project: "wms"
  tsum_pipeline_type: "wms-pr-check-ci-mt"
  robot_secret: "sec-01dgd1q7tdynkch7sdcf1taxyt"
  arcanum_review_id: "${context.launch_pull_request_info.pull_request.id}"
