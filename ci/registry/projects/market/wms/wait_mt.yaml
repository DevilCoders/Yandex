title: Ожидание запуска мультитестинга
description: Ждем пока мультитестинг поднимется и прогонятся автотесты
maintainers: serpentine
sandbox-task:
  name: WMS_WAIT_MTAUTOTESTS
  required-parameters:
    - ticket_id
    - branch
    - tsum_project
    - robot_secret

parameters:
  ticket_id: "${context.launch_pull_request_info.issues[0].id}"
  branch: "${context.launch_pull_request_info.vcs_info.feature_branch}"
  tsum_project: "wms"
  robot_secret: "sec-01dgd1q7tdynkch7sdcf1taxyt"
