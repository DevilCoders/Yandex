title: Market Alerts Configs Tests
description: Test alerts configuration
maintainers: marketito

sandbox-task:
  name: MARKET_ALERTS_CONFIGS_TESTS
  required-parameters:
    - test_type
    - malec_config_root
    - malec_config_globals

parameters:
  arcadia_url: "arcadia-arc:/#${context.target_revision.hash}"
  malec_token: "${context.secret_uid}#malec-token"
  malec_config_root: market/sre/conf/market-alerts-configs/configs
  malec_config_globals: market/sre/conf/market-alerts-configs/globals

