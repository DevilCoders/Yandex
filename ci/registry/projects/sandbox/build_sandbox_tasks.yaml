title: Build sandbox tasks
description: Build sandbox tasks
maintainers: sandbox
sandbox-task:
  name: BUILD_SANDBOX_TASKS
parameters:
  build_mode: semi_distbuild
  tasks_svn_url: "arcadia-arc:/#${context.target_revision.hash}"
