title: Сборка архива моделей для cfg_models
description: Собирает архив моделей, используя таску BUILD_CFG_MODELS_ARCHIVE
maintainers: relev_tools
sandbox-task:
  name: BUILD_CFG_MODELS_ARCHIVE

parameters:
  config_mode: arcadia
  arc_token: ${context.secret_uid}#ci.token
  yt_token: ${context.secret_uid}#ci.token
