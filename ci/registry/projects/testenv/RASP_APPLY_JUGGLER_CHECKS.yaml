title: RASP_APPLY_JUGGLER_CHECKS
description: Testenv job
maintainers: rasp

sandbox-task:
  name: ANSIBLE_JUGGLER_RUNNER

requirements:
  sandbox:
    platform: linux

parameters:
  environment: production
