title: Check and deploy rule monitoring
description: Testenv job
maintainers: bagiro44,kender
sandbox-task:
  name: DEPLOY_RULE_MONITORING
parameters:
  environment: production
  new_revision: arcadia:/arc/${context.branch}/arcadia@${context.target_revision.number}
  commit_author: ${context.target_commit.author}
