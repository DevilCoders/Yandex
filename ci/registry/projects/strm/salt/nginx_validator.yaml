title: Накатка солта и сборка тарбола с результатом
description: Накатка солта и сборка тарбола с результатом
maintainers: strm

sandbox-task:
  name: RUN_NGINX_STRM_VALIDATOR_FROM_PACKAGE
  required-parameters:
    - salt_states_resource
    - salt_minion_id
    - salt_file_roots_paths
    - salt_pillar_roots_paths
    
parameters:
  binary_executor_release_type: stable
