title: Внутренний релиз android sdk
description: Запуск TSR для сборки внутреннего релиза сдк
maintainers: mobileadssdk

sandbox-task:
  template: mobileadssdk_android_internal
  required-parameters:
    - env
    - commit

parameters:
  env: ~
  commit: ~
