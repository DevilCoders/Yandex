title: Внешний релиз android sdk
description: Запуск TSR для сборки внешнего релиза сдк
maintainers: mobileadssdk

sandbox-task:
  template: mobileadssdk_android_maven
  required-parameters:
    - env
    - commit

parameters:
  env: ~
  commit: ~
