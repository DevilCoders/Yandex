maintainers: s3
sources: /mds/s3/tasklets/teamcity
tasklet:
  implementation: S3TeamCityRunBuild
title: Запуск сборки в TeamCity и ожидание её завершения.
description: |
  Запускает сборку в TeamCity для нужного пакета.
  Ожидает, что в результате сборки будет опубликован артефакт со списком собранных пакетов и их версий в формате .json:
    [ { "name": "s3-goose", "version": "0.0.1" } ]
versions:
  stable: 3358580571
