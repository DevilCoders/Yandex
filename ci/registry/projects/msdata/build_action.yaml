title: BuildAction
description: Сборка ресурса с бинарником (Action)
maintainers: statkey

sandbox-task:
  name: BUILD_CALCULATION_RESOURCE

parameters:
    checkout_arcadia_from_url: "arcadia-arc:/#${context.target_revision.hash}"
    type: action
    targets:
        "bin/action": "${context.config_info.dir}"
    resource_description: "Action: ${context.config_info.dir}"
