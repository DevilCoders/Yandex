title: PCODE ZFP validation alert config
description: Run task that starts validation alert config
maintainers: pcode

sandbox-task:
    name: PCODE_ZFP_VALIDATE_ALERT_CONFIG
    required-parameters:
        - alert_config_name
        - pr_id
        - alert_package_resource
        - arcanum_token
