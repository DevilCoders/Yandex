title: Build deploy_notifications_controller
description: Build deploy notifications controller using BUILD_DEPLOY_NOTIFICATIONS_CONTROLLER task
maintainers: drug
parameters:
  checkout_arcadia_from_url: arcadia-arc:/#${context.target_revision.hash}
  run_tests: true
  service_version: ${context.target_revision.hash}.trunk
requirements:
  disk: 512MB
  sandbox:
    platform: linux
sandbox-task:
  name: BUILD_DEPLOY_NOTIFICATIONS_CONTROLLER
