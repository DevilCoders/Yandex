title: TeamcityApkBuildWrapper
description: Run TEAMCITY_APK_BUILD_WRAPPER task
maintainers: quasar_infra

sandbox-task:
    name: TEAMCITY_APK_BUILD_WRAPPER
    required-parameters:
        - config_path

parameters:
    branch: trunk
    commit: ${context.target_revision.hash}
