title: Samovar build package
description: Testenv job
maintainers: alexcoach
sandbox-task:
  name: BUILD_SAMOVAR_PACKAGE
parameters:
  environment: production
  build_system: ya
  notify_if_finished: ${context.target_commit.author}
  notify_via: email
  notify_if_failed: ${context.target_commit.author}
  checkout_arcadia_from_url: arcadia-arc:/#${context.target_revision.hash}
  build_type: release
requirements:
  sandbox:
    platform: any
  disk: 120GB
