---
title: BUILD_PORTO_LAYER job
description: Run BUILD_PORTO_LAYER sandbox task
maintainers: lazuka23

sandbox-task:
  name: BUILD_PORTO_LAYER
  required-parameters:
    - layer_type
    - layer_name
    - compress
    - start_os
# NOTE: BUILD_PORTO_LAYER task does not support arc revision hash.
# Use targetRevision.number in a.yaml file to select specific svn revision:
# EXAMPLE:
#   parameters:
#     script: "arcadia:/arc/trunk/arcadia/.../file.sh@${context.target_revision.number}"