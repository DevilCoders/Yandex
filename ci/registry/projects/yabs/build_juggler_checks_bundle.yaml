title: Build Juggler checks bundle
description: Сборка бандла Juggler-проверок
maintainers: lazuka23

sandbox-task:
  name: BUILD_JUGGLER_CHECKS_BUNDLE
  required-parameters:
    - package_path
    - bundle_name

parameters:
  checkout_arcadia_from_url: "arcadia:/arc/trunk/arcadia@${context.target_revision.number}"
  upload_to_mds: false
  resource_type: JUGGLER_CHECKS_BUNDLE
  force_resource: false
