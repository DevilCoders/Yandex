title: Релиз PR Афиши
description: Собирает и выкатывает PR Афиши на стейджинг
maintainers: msurin

sandbox-task:
  name: AFISHA_PR_RELEASE
  required-parameters:
    - arcanum_review_id
    - components

parameters:
  arcanum_review_id: "${context.launch_pull_request_info.pull_request.id}"
