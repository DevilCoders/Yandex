title: GenericLauncher
description: Run Sandbox GENERIC_LAUNCHER
maintainers: appmetricasdkduty

sandbox-task:
  name: GENERIC_LAUNCHER

parameters:
  branch: "${context.branch}"
  commit: "${context.target_revision.hash}"
  ssh_key_yav: "${context.secret_uid}"
