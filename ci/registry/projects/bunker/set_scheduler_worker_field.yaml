title: Replace worker in scheduler
description: Set scheduler named_resources worker using BUNKER_SET_SCHEDULER_CUSTOM_FIELD
maintainers: bunker

sandbox-task:
  name: BUNKER_SET_SCHEDULER_CUSTOM_FIELD
  required-parameters:
    - scheduler_id
    - name
    - mode
    - dictvalue

parameters:
  mode: dict
  name: named_resources

attempts: 1