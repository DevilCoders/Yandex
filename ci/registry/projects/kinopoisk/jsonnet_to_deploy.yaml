title: Jsonnet to Deploy
description: Делает преобразование jsonnet -> yaml и пушит его в Deploy.
maintainers: kinopoisk

sandbox-task:
  name: JSONNET_TO_DEPLOY
  required-parameters:
    - path_to_jsonnet
    - eps

parameters:
  docker_tag: "${context.target_revision.hash}"
