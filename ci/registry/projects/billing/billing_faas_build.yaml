title: BillingFaasBuildTask
description: Run Sandbox BILLING_FAAS_BUILD_TASK
maintainers: billing

sandbox-task:
  name: BILLING_FAAS_BUILD_TASK
  required-parameters:
    - env
    - service
  badges-configs:
    - id: report
      module: SANDBOX

parameters:
  env: test
  service: faas

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: HIGH

versions:
  stable: 3298149335
