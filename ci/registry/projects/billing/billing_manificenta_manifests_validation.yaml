title: BillingManificentaManifestsValidationTask
description: Run Sandbox BILLING_MANIFESTS_VALIDATION_TASK
maintainers: billing

sandbox-task:
  name: BILLING_MANIFESTS_VALIDATION_TASK
  required-parameters:
    - arcadia_hash

parameters:
  arcadia_hash: "${context.target_revision.hash}"

requirements:
  sandbox:
    priority:
      class: SERVICE
      subclass: NORMAL
