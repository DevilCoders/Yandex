title: RunJenkins
description: Run task in jenkins
maintainers: vr

tasklet:
    runtime: sandbox
    implementation: JenkinsRunner

versions:
    stable: 2754588996

parameters:
    config:
        jenkins_host: https://jenkins.vr-dev.yandex.net
        jenkins_credentials: sec-01fnr4v7j5jbjsq5zekhmb0xtq

attempts: 1

requirements:
    disk: 1GB
    cores: 1
    ram: 2GB
    
    sandbox:
        client_tags: SSD
        priority:
            class: SERVICE
            subclass: HIGH

