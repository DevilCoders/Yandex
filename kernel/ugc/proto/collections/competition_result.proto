syntax = "proto3";

package NUgc.NSchema.NCollections;

enum ECompetitionStatus {
    NEW = 0;
    IN_CHECK = 1;
    VERIFIED = 2;
    DECLINED = 3;
}

// TODO: document, refactor more.
message TCompetitionResult {
    ECompetitionStatus Status = 1;
    string Reference = 2;
    int64 Place = 3;
}
