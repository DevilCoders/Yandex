syntax = "proto3";

package NUgc.NSchema;

message TLikedVideo {
    string OntoId = 1;                      // video id from ontodb

    oneof LikeDislike {
        bool Like = 2;                      // video is liked
        bool Dislike = 3;                   // video is disliked
    }
}
