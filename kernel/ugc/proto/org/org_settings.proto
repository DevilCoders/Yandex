syntax = "proto3";

package NUgc.NSchema.NOrgs;

import "google/protobuf/wrappers.proto";

message TUserOrgSettings {
    // User explicitly answered that he own or not some organization (for SMB features)
    google.protobuf.BoolValue PossiblyOwnSomeOrg = 1;
}
