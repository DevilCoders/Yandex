syntax="proto3";

package NUgc.NSchema;

// For each Yandex service we store whether a user has created some content
message TServiceUsage {
    bool Ugc = 1;
    bool District = 2;
    bool Znatoki = 3;
}
