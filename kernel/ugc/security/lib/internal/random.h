#pragma once

#include <cstddef>

namespace NUgc {
    namespace NSecurity {
        namespace NInternal {
            /// Generates pseudo-random bytes sequence. @buf parameter should be at least @num size.
            /// Returns number of generated bytes.
            size_t RandomBytes(char* buf, size_t num);
        } // namespace NInternal
    } // namespace NSecurity
} // namespace NUgc
