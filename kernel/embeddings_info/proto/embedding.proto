syntax = "proto3";

package NEmbedding;

option go_package = "a.yandex-team.ru/kernel/embeddings_info/proto;embeddings";

message TEmbedding {
    enum EFormat {
        Compressed = 0;
        Decompressed = 1;
    };

    bytes Embedding = 1;
    int32 FirstVersion = 2;
    int32 LastVersion = 3;
    float Multipler = 4; // technical field for compressing/decompressing
    EFormat Format = 5;
    float Norm = 6; // save norm value here
};
