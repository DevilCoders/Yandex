#pragma once

#include <util/system/defaults.h>

static const size_t XMapWordBits = 6;  // Количество бит, отводимых для хранения слов в тексте ссылки, WORD_LEVEL_Bits
static const size_t XMapLinkBits = 15; // Количество бит, отводимых для хранения всех ссылок на документ, BREAK_LEVEL_Bits
