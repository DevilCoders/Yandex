Библиотека сегментатора.

Используется для выделения и классификации сегментов в документе. Нумератор содержит 
расширение интерфейса нумератора, позволяющее наследующим нумераторам в OnTextEnd 
или ниже получать копию разметки с набором признаков. Признаки и разметка могут 
сериализоваться в текстовые архивы. Конечное разбиение на сегменты можно доукрупнять 
и переклассифицировать.

см. также kernel/segmentator, kernel/segutils, tools/segutils 