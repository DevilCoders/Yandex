LIBRARY()

OWNER(g:cs_dev)

PEERDIR(
    kernel/common_server/settings/abstract
    kernel/common_server/settings/fake
    kernel/common_server/settings/db
    kernel/common_server/settings/segments
)

SRCS(
)

END()
