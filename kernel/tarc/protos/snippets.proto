package NOxygen;

message TSnippetData {
    optional string Type = 1;
    optional string Text = 2;
    optional bool IsFromBeauty = 3; // Temporal flag, used in Jupiter canonization
}

message TSnippetDataArray {
    repeated TSnippetData Data = 1;
}
