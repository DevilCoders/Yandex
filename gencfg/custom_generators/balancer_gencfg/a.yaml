ci:
  release-title-source: flow
  actions:
    build_balancer_configs:
      flow: build_balancer_configs
      triggers:
      - filters:
        - sub-paths:
          - configs/addrs/**
          - configs/gencfg/**
        'on': commit
  flows:
    build_balancer_configs:
      jobs:
        ON_DEMAND_BUILD_BALANCER_CONFIGS:
          task: projects/testenv/ON_DEMAND_BUILD_BALANCER_CONFIGS
          title: ON_DEMAND_BUILD_BALANCER_CONFIGS
      title: build_balancer_configs
  runtime:
    sandbox-owner: SEARCH-RELEASERS
  secret: sec-01fag5j1kcvr7nb4h920nyx736
service: gencfg
title: Autogenerated a.yaml
