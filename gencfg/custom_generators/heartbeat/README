Плагины для skynet-а генерируются автоматически из кода генерилки (чтобы избежать дубликации) скоптом ${ROOT}/gen-heartbeat-plugins.sh. После генерации они ложатся в generated/hplugins. После генерации их нужно положить в svn+ssh://arcadia.yandex.ru/arc/trunk/arcadia/skynet/services/heartbeat-client/src/plugins .

Отдельный скрипт можно собрать командой ${ROOT}/heartbeat/generate_plugin.py -p instanceusage -o ex.py
