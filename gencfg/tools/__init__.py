"""
    Bigger tools, than usual utils:
        - analyzer: scripts to gather current statistics (e. g. rps, instance/host cpu usage, disk usage);
        - diffbuilder: scripts to generate gencfg diff to previous tag
        - recluster: scripts to recluster group according to information in recluster section of group card
        - cfggen: scrips to build configs for specified groups
"""
