## Optimize ##
1. Prepare target power weights(configs/{location}.yaml
2. 


Stage 1. {location}.sh start

This stage destroys intlookups and slave groups
After this stage you can allocate group from XYZ_WEB_BASE that cannot be allocated from XYZ_RESERVED

Stage 2. {location>}.sh mid

Allocate hosts for project.

Stage 3. {location}.sh end

Start intlookups generation stage. The longest one.
