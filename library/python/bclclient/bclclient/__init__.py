from .client import Bcl
from .settings import HOST_TEST, HOST_PRODUCTION


VERSION = (0, 1, 0)
"""Application version number tuple."""

VERSION_STR = '.'.join(map(str, VERSION))
"""Application version number string."""
