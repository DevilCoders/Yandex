service: passport_infra
title: ticket_parser2 (python)

ci:
  release-title-source: flow
  autocheck:
    fast-targets:
      - library/python/deprecated/ticket_parser2
      - passport


arcanum:
  auto_merge:
    enabled: true
    requirements:
      - system: pciexpress
        type: PCI-DSS integrity
