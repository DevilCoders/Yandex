message TestMessage {

    optional int32 MessageId = 1;

    optional float Ratio = 2;
    optional uint64 Key = 3;
    repeated string Tags = 4;
    optional string Name = 5;

    enum EService {
        Cs        = 0;
        Bstr      = 1;
        Validator = 2;
    }

    optional EService Service = 6;
    optional string Info = 7;
}
