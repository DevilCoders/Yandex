package NProtoParser;

import "google/protobuf/descriptor.proto";

extend google.protobuf.FieldOptions {
    optional string delimiter = 55000; // used for repeated fields when there is need to specify many values in one string
}
