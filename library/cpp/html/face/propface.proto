package NHtml;

message TDocumentProperties {
    message TProperty {
        optional string Name = 1;
        repeated string Values = 2;
    }

    repeated TProperty Properties = 1;
    repeated string Links = 2;
}
