service: passport_infra
title: auth client parser (c++)

arcanum:
  review:
     auto_assign: true

     groups:
        - name: backend-developers
          roles: developer

     rules:
        - reviewers:
            name: backend-developers
            ship: 2
            assign: 2

ci:
  release-title-source: flow
  autocheck:
    fast-targets:
      - library/cpp/auth_client_parser
      - library/python/auth_client_parser
    strong: true
