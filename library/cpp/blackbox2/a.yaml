service: passport_infra
title: libblackbox2

ci:
  release-title-source: flow
  autocheck:
    fast-targets:
      - library/cpp/blackbox2
      - passport
    strong: true

arcanum:
  auto_merge:
    enabled: true
    requirements:
      - system: arcanum
        type: comment_issues_closed
