parsing:
  groups: [disk_mqueue]
  metahost: disk_mqueue
  DataFetcher:
      type: http
      port: 3132
      uri: "/exec_pattern?pattern=rabbitmq_connections_stats"
aggregate:
  data:
    rabbitmq_connections_stats:
      type: "custom"
      class: "Multimetrics"
      rps: no
      perHost: YES 


  senders:
    graphite:
      cluster: "media.disk"
      type: graphite


