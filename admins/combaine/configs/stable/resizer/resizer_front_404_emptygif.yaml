parsing:
  groups: [resizer-front]
  DataFetcher:
    timetail_url: "/timetail?pattern=resizer_404_emptygif&type=common&log_ts="
aggregate:
  data:
    resizer_emptygif:
      type: custom
      class: Multimetrics
  senders:
    graphite:
      type: graphite
      cluster: media.resizer
