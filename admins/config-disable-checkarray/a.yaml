service: nocdev
title: config-disable-checkarray

ci:
  release-title-source: flow
  autocheck:
    fast-targets:
      - admins/config-disable-checkarray

  secret: sec-01f00xrpf9sfcd6fjrdrzw8tj3

  runtime:
    sandbox-owner: NOCDEV

  releases:
    release:
      title: Release config-disable-checkarray
      flow: release
      stages:
        build:
          title: Build config-disable-checkarray package

  flows:
    release:
      title: Release config-disable-checkarray package
      jobs:
        build:
          stage: build
          title: Build config-disable-checkarray package
          description: Build config-disable-checkarray package and upload it to dist
          task: common/arcadia/ya_package
          input:
            packages: admins/config-disable-checkarray/pkg.json
            build_type: release
            package_type: debian
            publish_package: true
            publish_to: common
            debian_distribution: unstable
            resource_type: YA_PACKAGE
            use_aapi_fuse: true
            use_arc_instead_of_aapi: false
            aapi_fallback: true
            use_new_format: true
            compress_package_archive: false
            key_user: robot-nocdeploy
