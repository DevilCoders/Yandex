#log_format defaultformat '[$time_local] $http_host $remote_addr "$request" $status "$http_referer" "$http_user_agent" "$http_cookie" $request_time $upstream_cache_status $bytes_sent "$upstream_response_time" "$uri" "$args" $ssl_session_id $sent_http_location';
#access_log      syslog  defaultformat;
tskv_log_format mirror "type=nginx unixtime_microsec_utc=$msec request_time=$request_time upstream_cache_status=$upstream_cache_status upstream_response_time=$upstream_response_time ssl_session_id=$ssl_session_id ssl_handshake_time=$ssl_handshake_time request_length=$request_length bytes_sent=$bytes_sent";
#log_format defaultformat '[$time_local] $http_host $remote_addr "$request" $status "$http_referer" "$http_user_agent" "$http_cookie" $request_time $upstream_cache_status $bytes_sent';
tskv_log  /var/log/nginx/tskv.log  mirror;
