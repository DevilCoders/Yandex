server {
    server_name teamcity-autotests.media.yandex.net;
    listen    [::]:80;
    location / {
        include         uwsgi_params;
        uwsgi_pass unix:/var/run/teamcity-autotests-monitoring.sock;
    }

    location /ping {
        return 200;
    }
}
