# this checks asks services locally via ping urls

[weather-quality.y-t.ru-ping]
execution_interval=20
command=/usr/bin/jhttp.sh -n weather-quality.yandex-team.ru -u /ping

[weather-editor-ping]
execution_interval=20
command=/usr/bin/jhttp.sh -n weather.yandex-team.ru -u /ping
