#!/usr/bin/make -f

override_dh_install:
	mkdir -p debian/tmp/usr/share/rsyslog-repo-config
	mkdir -p debian/tmp/etc/apt/sources.list.d
	cp rsyslogkey.gpg debian/tmp/usr/share/rsyslog-repo-config
	cp rsyslog-repo-config.list debian/tmp/etc/apt/sources.list.d
	dh_install

%:
	dh $@ 
