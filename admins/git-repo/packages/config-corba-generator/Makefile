PKGNAME = config-corba-generator
DATADIR = /usr/share
YCONFDIR = $(DESTDIR)$(DATADIR)/yandex-configs/$(PKGNAME)

#clean:
#	@rm -rf debian/tmp

install: 
#install example configs and make file
	install -m644 src/etc/rsyncd.conf $(DESTDIR)/etc/
