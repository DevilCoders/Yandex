log_format toolsformat '[$time_local] $http_host $remote_addr "$request" $status "$http_referer" "$http_user_agent" "$cookie_yandex_login" "yandexuid=$cookie_yandexuid" $request_time $upstream_cache_status $bytes_sent';

access_log /var/log/nginx/access.log toolsformat;
