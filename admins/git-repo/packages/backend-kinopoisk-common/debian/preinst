#!/bin/bash

# clear conf
# CONFFILES cannot be deleted manually!!
rm -rf /etc/apache2/sites-enabled/*
# disable ldap
cat /etc/nsswitch.conf | sed 's/ldap//g' > /etc/nsswitch.conf.new
mv /etc/nsswitch.conf.new /etc/nsswitch.conf
