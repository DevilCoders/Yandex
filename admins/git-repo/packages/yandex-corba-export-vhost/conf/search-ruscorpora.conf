$HTTP["host"] =~ "^search\.ruscorpora(\.\w+\.yandex)?\.ru" {
        server.document-root = "/usr/local/www5/ruscorpora"
        $HTTP["url"] =~ "^/x(m|s)l" {
                url.access-deny = ( "" )
        }
}
$HTTP["host"] =~ "^search-beta\.ruscorpora(\.\w+\.yandex)?\.ru" {
        server.document-root = "/usr/local/www5/ruscorpora-beta"
        $HTTP["url"] =~ "^/x(m|s)l" {
                url.access-deny = ( "" )
        }
}
