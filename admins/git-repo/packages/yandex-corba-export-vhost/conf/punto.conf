$HTTP["host"] =~ "^(www\.)?punto(-switcher)?\.ru" {
        url.redirect = (
                ".*" => "http://punto.yandex.ru"
        )
}

$HTTP["host"] =~ "^punto(\.\w+)?\.yandex\.ru" {
	server.document-root = "/usr/local/www5/punto"
}
