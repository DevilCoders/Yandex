#!/usr/bin/make -f
override_dh_install:
	mkdir -p debian/config-nginx-addon-filelog-defaultformat/etc/nginx/sites-enabled
	cp -r nginx debian/config-nginx-addon-filelog-defaultformat/etc/

%:
	dh $@

