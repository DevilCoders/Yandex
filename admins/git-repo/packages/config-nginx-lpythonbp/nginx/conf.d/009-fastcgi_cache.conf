fastcgi_cache_path  /var/tmp/ levels=1:2 keys_zone=one:1024m max_size=2G inactive=1h;
fastcgi_cache_min_uses 10;
fastcgi_cache_use_stale error timeout invalid_header http_500;
fastcgi_cache_valid 200 20m;

