#!/usr/bin/make -f
override_dh_install:
	mkdir -p debian/deploy-list-checker/usr/bin/
	cp deploy-checker.sh debian/deploy-list-checker/usr/bin/deploy-checker.sh
	chmod +x debian/deploy-list-checker/usr/bin/deploy-checker.sh

%:
	dh $@

