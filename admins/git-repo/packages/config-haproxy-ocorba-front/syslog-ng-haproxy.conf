filter f_haproxy { (program("haproxy")); };
destination df_haproxy { file("/var/log/haproxy.log" perm(0644) dir_perm(0755) owner(root) dir_owner(root)); };
log {
	source(s_src);
	filter(f_haproxy);
	destination(df_haproxy);
	flags(final);
};

