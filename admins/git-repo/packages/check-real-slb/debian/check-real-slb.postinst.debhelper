# Automatically added by dh_installinit
if [ -x "/etc/init.d/check-real-slb" ]; then
	if [ ! -e "/etc/init/check-real-slb.conf" ]; then
		update-rc.d check-real-slb defaults >/dev/null
	fi
	invoke-rc.d check-real-slb start || exit $?
fi
# End automatically added section
