upstream sentry_api {
#        server adminki01g.tools.yandex.net max_fails=2 fail_timeout=10s;
#	server adminki01e5.tools.yandex.net max_fails=2 fail_timeout=10s;
	server 127.0.0.1:8081;
}

server {
       include listen;
       server_name sentry.admin.tools.yandex.net sentry.tools.yandex.net;
       location / {
       		proxy_pass http://sentry_api;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		}

}

