log file = /var/log/rsyncd.log
pid file = /var/run/rsyncd.pid
lock file = /var/run/rsync.lock
uid=root
gid=root
[backup]
        path = /backup
        comment =  backup area
        read only = false
	timeout = 300

