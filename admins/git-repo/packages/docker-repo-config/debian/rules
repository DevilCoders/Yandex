#!/usr/bin/make -f

override_dh_install:
	mkdir -p debian/tmp/usr/share/docker-repo-config
	mkdir -p debian/tmp/etc/apt/sources.list.d
	cp dockerkey.gpg debian/tmp/usr/share/docker-repo-config
	cp docker-repo-config.list debian/tmp/etc/apt/sources.list.d
	dh_install

%:
	dh $@ 
