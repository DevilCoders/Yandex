# TODO: починить
server {
        include listen;
	server_name collection.yandex.ru;
        server_name ~^(www\.)?collection\.[a-z0-9\-]+\.yandex\.ru$;
        root /usr/local/www5/collection;
	include xscripts/default;
	location ~^/((blocks|pages|lego|i|i-static)/.*) { }
	location = /robots.txt { }
	location / {
		rewrite ^/?(index.xml)?(\?(.*))? /pages/index/index.xml$2 last;
		}
}
