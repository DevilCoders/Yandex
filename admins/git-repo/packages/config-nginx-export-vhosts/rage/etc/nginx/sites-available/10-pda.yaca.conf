# TODO разобраться почему не работает.
server {
	include listen;
	server_name pda.yaca.yandex.ru;
	server_name ~^pda.yaca\.[a-z0-9\-]+\.yandex\.ru$;
	root /usr/local/www5/pda.yaca;
	include fastcgi_params_yandex;
	include xscripts/yaca;
	include locations/locations-xscript-yaca;
	location ~* ^/yca/([\w-]+)? {
		fastcgi_pass unix:/tmp/xscript-multiple/xscript-yaca.sock;
		include fastcgi_params_yandex;
		}

}

server {
        include listen;
	server_name www.pda.yaca.yandex.ru;
	rewrite .* http://pda.yaca.yandex.ru/yca/cat permanent;
}

