server {
	include listen;
	server_name mobile.yandex.ru www.mobile.yandex.ru;
	server_name ~^(www\.)?mobile\.[a-z0-9\-]+\.yandex\.ru$;
	root /usr/local/www5/mobile;
#	include fastcgi_params_yandex;
	location ~^/(css|js|lego|i) { }
	location ~^/maps/promo { 
		alias /usr/local/www5/mobile-maps-promo; }
	location ~^/kyivstar {
		include xscripts/default;
		alias /usr/local/www5/mobile-kyivstar;
		index index.xml;
#		fastcgi_pass    unix:/tmp/xscript-multiple/xscript-default.sock;
		}
#	rewrite ^/online/? /mail/ permanent;
#	rewrite ^/ymail/? /mail/promo/ permanent;
	#rewrite ^/kyivstar$ /kyivstar/ permanent; 
	# TODO дописать все редиректы. в т.ч. $0
	#rewrite ^/((css|js|lego|i)/.*) $1 last;
	include xscripts/default;
	include locations/robots-xscript-default;

}

