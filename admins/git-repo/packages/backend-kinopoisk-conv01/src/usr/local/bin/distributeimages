#!/usr/bin/env bash

if [ $# -lt 1 ]; then
        echo "Usage: $0 PATH"
        exit 1
fi

URL=$1

st5="st5"

rsync -a $URL www-data@$master01h.kp.yandex.net:$URL
if [ "$?" -eq 1 ]; then echo "ERROR"; else echo "OK, remove" && rm $URL; fi
