# first argument to /usr/bin/haproxy-backends-check.sh  - type of check (see examples below)
# second argument - path to haproxy stats socket.

# checks, that there are no backends with 50% RS or more down. 
# if are = crit with list of backends.
[haproxy-backends]
execution_interval=30
command=/usr/bin/haproxy-backends-check.sh get_percentage_of_down_rs_crit /tmp/haproxy-stat.sock

# checks, that there are no backends with 100% RS down
# if are = crit.
[haproxy-backends-achtung]
execution_interval=30
command=/usr/bin/haproxy-backends-check.sh get_percentage_of_down_rs_achtung
