#!/bin/bash

MONGO_BACKUP_ENABLED=0 # If backup is enabled on current machine
MAX_RSYNC_RETRIES=10 # Retries rsync to backup server.

STANDALONE=0 # There is only one machine, don't bother it is master.

R_PASSWORD='fvgbhnjm' # Password for rsync
R_REALM='mongo-backup' # Rsync share name
R_SERVER='localhost' # Rsync server
R_USER='maps' # Rsync username

MONGO_DUMP_PORT=27018 # Port where mongodb lives
MONGO_MONGOS_PORT=27017 # Port where mongos lives
MONGO_DATA_DIR='/var/lib/' # Dir where data mongo stored
MONGOS_HOST='localhost' # Host where mongos lives

ZK_FLOCK_CONFIG='/etc/distributed-flock-mongo-backup.json' # zk-flock config to use

BACKUP_OPLOG=0 # If you want to backup oplog

SHARD_NAME='' # Arbitrary name of current shard, if empty - use replica set name


M_USER="" # User for authentication
M_PASSWD="" # User's password
