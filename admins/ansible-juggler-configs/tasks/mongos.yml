- include: media_basic.yml
  vars:
    service_tags:
      - mongos
      - specific
    checks:
      - mongos-alive
      - mongos-pool

- include: media_basic.yml
  vars:
    service_tags:
      - mongos
      - specific
    type: warning
    checks:
      - mongos-uptime

