- include: tasks/media.yml
  vars:
    service_tags:
      - common
- include: tasks/media_basic.yml
  vars:
    nodata_mode: force_crit
    service_tags:
      - common
    checks:
      - monitoring-alive
