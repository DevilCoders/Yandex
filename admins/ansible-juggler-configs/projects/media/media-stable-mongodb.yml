- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    host: media-stable-mongodb
    host_tags:
    - media-stable-mongodb

  pre_tasks:
  - juggler_facts: jserver_api={{ jserver_search_api }}
  - include_vars: "vars_alet"
 
  tasks:
  - include: tasks/unreachable.yml
  - include: tasks/common-check.yml
  - include: tasks/mongodb.yml

  post_tasks:
  - juggler_cleanup: jcheck_mark={{ host }}