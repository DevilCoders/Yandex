- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    type: testing
    host: cult-stable-backup
    host_tags:
    - cult-stable-backup

  pre_tasks:
  - juggler_facts: jserver_api={{ jserver_search_api }}
  - include_vars: "vars"
 
  tasks:
  - include: tasks/unreachable.yml
  - include: tasks/common-check.yml

  post_tasks:
  - juggler_cleanup: jcheck_mark={{ host }}
