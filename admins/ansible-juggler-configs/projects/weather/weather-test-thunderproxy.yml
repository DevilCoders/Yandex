- hosts: localhost
  gather_facts: False
  vars:
    host: weather-test-thunderproxy
    type: testing
    host_tags:
      - test

  pre_tasks:
    - juggler_facts: jserver_api={{ jserver_search_api }}
    - include_vars: "vars"
  post_tasks:
    - juggler_cleanup: #jcheck_mark={{ host }} # commented for clean up all checks made with playbook

  tasks:
    - include: tasks/yasm.yml
      vars:
        service_tags:
          - specific
        checks:
          weather.alerts.test.thunderproxy.diskusage.cwd.percentage: unispace_cwd
          weather.alerts.test.thunderproxy.diskusage.root.percentage: unispace_root
          weather.alerts.test.thunderproxy.diskusage./logs.percentage: unispace_logs

