JUGGLER_ROOT="/etc/yandex/juggler-client"
JUGGLER_USER="{{juggler_user}}"
DAEMON_USER="{{juggler_daemon_user}}"
{% if juggler_hack_porto %}
function command {
    if [ ${@: -1} != "portoctl" ]; then
        builtin command $@
    else
        false
    fi
}
{%- endif -%}

