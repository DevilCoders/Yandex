{% if salt['pillar.get']("hw-watcher:oauth-token") -%}
{% set token = salt['pillar.get']("hw-watcher:oauth-token") -%}
{% else -%}
{% set token = pillar['yav']['robot-storage-duty_bot_token'] -%}
{% endif -%}
[bot]
oauth_token = {{ token }}
