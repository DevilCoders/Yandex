	if ($host ~* ^www\.(.*)) {
		set $host_nowww $1;
		rewrite ^(.*) $scheme://$host_nowww$1 permanent;
	}

	if ($host ~* (.*)\.com\.ua$) {
		set $host_nocomua $1;
		rewrite ^(.*) $scheme://$host_nocomua$1 permanent;
	}
