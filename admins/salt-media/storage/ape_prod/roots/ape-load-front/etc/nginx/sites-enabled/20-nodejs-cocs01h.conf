upstream nodejs-cocs01h {
        server cocs01h.dev.ape.yandex.net:8123 fail_timeout=3s;
	keepalive 1024;
}

server {
	server_name ~nodejs-front01h.(dev|load).ape.yandex.net;
	listen 80;
	listen [::]:80;
	location / {
		proxy_pass http://cocaine-native;
		proxy_http_version 1.1;
        	proxy_set_header Connection "";
	}
}
