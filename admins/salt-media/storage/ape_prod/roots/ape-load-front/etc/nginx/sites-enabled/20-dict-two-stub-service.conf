# it exists for https://st.yandex-team.ru/BOBR-1138
server {
	server_name translate.yandex.net;
	listen   80; 
	listen   [::]:80 ipv6only=on;

	root /srv/dict-two-service-stub;

	location / {
		rewrite .* /static_response;
	}

	location = /static_response {}
}
