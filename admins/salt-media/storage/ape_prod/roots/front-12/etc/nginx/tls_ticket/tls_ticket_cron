# get tickets from secdist put, rotate it /etc/nginx/ssl/tls/ and reload nginx
_GET_TICKETS=/usr/sbin/get-secdist-keys.sh
_KEY_NAME=tickets/ape.key
_KEY_DIR=/etc/nginx/ssl/tls/
ROBOT='robot-media-salt'
7 * * * * root /bin/bash $_GET_TICKETS -R -r reload_nginx cocaine $_KEY_NAME $_KEY_DIR
