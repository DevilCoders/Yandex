{% set fqdn = grains['fqdn']-%}
{% set dc = grains['conductor']['root_datacenter']-%}
{% set shard = fqdn | regex_replace('^.*?(\d+).*$', 'shard_\\1') %}
{% set replica = dc %}
<yandex>
	<macros>
		<shard>{{ shard }}</shard>
		<replica>{{ replica }}</replica>
	</macros>
</yandex>
