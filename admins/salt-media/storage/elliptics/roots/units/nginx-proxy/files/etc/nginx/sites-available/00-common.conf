server {
    server_name nonexisting;
    listen   *:80;
    listen   [::]:80;

    listen   *:443 ssl;
    listen   [::]:443 ssl;

    include ssl/https.conf;

    location /ping {
        proxy_pass http://mediastorage;
    }

    location / {
        return 500;
    }
}
