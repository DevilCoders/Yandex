location /get-docker-registry/ {
    auth_sign_token "{{ pillar['yav']['storage-auth_sign_token-docker-registry'] }}";
    include include/mediastorage-settings.conf;

    set $unistat_namespace "docker-registry";
    set $unistat_request_type "get";
}
