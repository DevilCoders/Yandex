tskv_log_format ydisk-downloader-access-log "http_x_forwarded_for=$http_x_forwarded_for upstream_addr=$upstream_addr upstream_response_time=$upstream_response_time request_id=$request_id upstream_status=$upstream_status upstream_cache_status=$upstream_cache_status $tcpinfo_rtt $tcpinfo_rttvar $tcpinfo_snd_cwnd $tcpinfo_rcv_space range=$http_range request_length=$request_length bytes_sent=$bytes_sent request_time=$request_time";
