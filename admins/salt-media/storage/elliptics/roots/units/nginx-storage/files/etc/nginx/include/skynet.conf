location /get-skynet/ {
    auth_sign_token "{{ pillar['yav']['storage-auth_sign_token-skynet'] }}";
    include include/mediastorage-settings.conf;

    set $unistat_namespace "skynet";
    set $unistat_request_type "get";
}
