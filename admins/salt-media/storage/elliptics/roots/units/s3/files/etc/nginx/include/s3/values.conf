{%- set env = grains['yandex-environment'] -%}

# These are variables required for S3Lib (s3/lib.lua).
set $s3_lib_initialized "0";  # S3Lib.init() was called for request
set $s3_bucket_name "";
set $s3_bucket_name_for_log "";
set $s3_public_request "0";
set $s3_yarl_env "{{ 't' if env == 'testing' else 'p' }}";
