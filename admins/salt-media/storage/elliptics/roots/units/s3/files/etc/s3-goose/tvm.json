{%- set env = grains['yandex-environment'] -%}

{%- if env == 'testing' -%}

{%- set client_id = 2017577 -%}
{%- set secret = pillar['tvm_secret_testing'] -%}

{%- else -%}

{%- set client_id = 2017579 -%}
{%- set secret = pillar['tvm_secret_prod'] -%}

{%- endif -%}

{
    "tvm2": {
        "client_id": {{ client_id }},
        "services_cache_ttl_sec": 3600,
        "secret": "{{ secret }}",
        "abc": {
            "base_url": "https://abc-back.yandex-team.ru/",
            "oauth_token": "{{ pillar['yav']['s3-idm-staff-oauth-token'] }}"
        }
    }
}

