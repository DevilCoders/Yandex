{%- set cached_buckets_GET = [
    "autoru-mag-data",
    "tanker"
] %}

{%- for bucket in cached_buckets_GET %}
    ~^(GET):{{ bucket }}\.s3\.yandex\.net:/.+ 1;
{%- endfor %}

~^(GET):market-idx\.s3\.mds\.yandex\.net:/fast_data_outlets.pbuf.sn 1;
