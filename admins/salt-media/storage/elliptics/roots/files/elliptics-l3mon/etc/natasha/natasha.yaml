{% set kitty_hosts = salt['conductor']['groups2hosts'](grains['conductor']['group']) %}
logging:
  level: debug
  sinks:
    - "stderr"

system_app:
  addr: ":8043"

http:
  addr: ":7742"

kitty:
  client:
    port: 7743
  discoverer:
    hosts: {{kitty_hosts | list}}
