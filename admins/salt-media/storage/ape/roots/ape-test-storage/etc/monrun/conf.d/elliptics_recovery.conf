[elliptics-recovery]
execution_interval=60
command=if [ `find /run/lock/elliptics_merge.lock -type f -mtime -2 | wc -l` == 0 ] || [ `find /run/lock/elliptics_dc.lock -type f -mtime -12 | wc -l` == 0 ];then echo "2;merge or dc start too old";else echo "0;Ok";fi
