source net { udp(port(6666)); udp6(port(6666)); };
destination netconsole { file("/opt/storage/syslog/$HOST-netconsole.log"); };

log {
    source(net);
    destination(netconsole);
    flags(final);
};
