- host: kino-back.content.yandex.net
  check_template: balancers.yaml
  checks:
      ping:
        active: http
        active_kwargs:
            path: /ping
            ok_codes: '200'
            port: 80
            disable_ipv4: True
      cobjects:
        active: http
        active_kwargs:
            path: /v5/cobjects/84342
            ok_codes: '200'
            port: 80
            disable_ipv4: True
