- host: tv-back.content.yandex.net
  check_template: balancers.yaml
  checks:
      ping:
        active: http
        active_kwargs:
            path: /ping
            ok_codes: '200'
            port: '80'
            disable_ipv4: True
