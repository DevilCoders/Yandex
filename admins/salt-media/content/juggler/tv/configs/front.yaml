- group: CGROUP%tv-stable-front
  tags:
    - front
  check_template: production.yaml
  base: base-back.yaml
  checks:
    nginx-50x:
    tv_499:

- group: CGROUP%tv-test-front
  check_template: nonproduction.yaml

