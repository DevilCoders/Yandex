use chroot = no
log file = /var/log/rsync.log
max verbosity = 256
pid file = /var/run/rsyncd.pid

[cores]
  uid = cores
  gid = cores
  read only = no
  path = /home/cores/Desktop/cores
  comment = Core Dumps and Hprofs

