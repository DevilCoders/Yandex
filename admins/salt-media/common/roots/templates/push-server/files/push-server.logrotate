/var/log/push-server-lite.log {
        weekly
        missingok
        copytruncate
        rotate 12
        compress
        notifempty
        postrotate
		/bin/kill -HUP `cat /var/run/statbox/push-server-lite.pid 2>/dev/null` 2>/dev/null || true
        endscript
}
