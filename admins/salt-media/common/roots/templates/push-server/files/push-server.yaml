logger:
  mode: [stderr, file]
  file: {{ logfile }}
  level: {{ loglevel }}
network:
  port: {{ netport }}
  hosts:
    - name: {{ nethost }}
storage:
  path: {{ rootpath }}
  rotate: {{ rotate }}
  filescount: {{ filescount }}
  compress: {{ compress }}
