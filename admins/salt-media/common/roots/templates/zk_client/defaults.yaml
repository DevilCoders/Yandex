{% set app_id = salt['grains.get']('conductor:project', 'localhost') %}
lookup:
  configs:
    /etc/distributed-flock.json:
  default:
      host:
        - localhost:2181
      timeout: 5
      app_id: {{ app_id }}
      sleep: "OFF"
      maxla: -1
