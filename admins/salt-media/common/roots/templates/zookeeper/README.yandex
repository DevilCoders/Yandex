По умолчанию формула использует кондукторный модуль.
Хосты для кластера беруться из переменной

zookeeper:
    cluster: {{ grains.get("conductor:group") }}

Туда можно передать имя группы или оставить дефолтное значенние (имя группы ;-) )
