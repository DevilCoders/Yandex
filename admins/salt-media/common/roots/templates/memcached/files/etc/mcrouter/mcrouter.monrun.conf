{%- set port = salt['pillar.get']('memcached:mcrouter:port', 5000) -%}
[mcrouter-alive]
execution_interval=60
execution_timeout=120
command=if [[ ! -z "$(echo stats | nc -w 5 0 {{port}} | egrep "STAT (child_)?pid")" ]]; then echo "0;OK"; else echo "2; Mcrouter is dead"; fi
type=memcached
