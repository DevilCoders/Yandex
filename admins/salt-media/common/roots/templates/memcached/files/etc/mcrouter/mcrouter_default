{%- set port = salt['pillar.get']('memcached:mcrouter:port', 5000) -%}
{%- set args = salt['pillar.get']('memcached:mcrouter:run_args', '') -%}
PORT={{port}}
MCROUTERCONF="/etc/mcrouter/mcrouter.conf"
LOGFILE="/var/log/mcrouter/mcrouter.log"
{% if args != '' -%}
PROG_ARGS="{{args}}"
{% endif %}
