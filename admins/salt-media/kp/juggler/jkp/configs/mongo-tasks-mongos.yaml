- group: CGROUP%jkp-stable-db-mongo-tasks-mongos
  tags:
    - conductor
    - jkp
    - mongos
  check_template: production.yaml
  base: base.yaml
  checks:
    mongos-alive:
    mongos-pool:
    mongos-uptime:

- group: CGROUP%jkp-test-db-mongo-tasks-mongos
  check_template: nonproduction.yaml
