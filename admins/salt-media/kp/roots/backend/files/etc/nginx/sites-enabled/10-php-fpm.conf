{% set yaenv = grains['yandex-environment'] -%}

{# old kinopoisk 'defaults' #}
{% set fastcgi_read_timeout = "90s" -%}
{% set fastcgi_send_timeout = "60s" -%}
{% set fastcgi_connect_timeout = "60s" -%}

{% if yaenv == 'development' %}
{% set fastcgi_read_timeout = "300s" -%}
{% set fastcgi_connect_timeout = "60s" -%}
{% endif %}

{# CADMIN-6143 #}
{% if salt["grains.get"]("conductor:group") in ["kp-mobile-api", "kp-prestable-mobile-api", "kp-test-mobile-api", "kp-load-mobile-api"] %}
{% set fastcgi_read_timeout = "15s" -%}
{% set fastcgi_send_timeout = "15s" -%}
{% set fastcgi_connect_timeout = "1s" -%}
{% endif -%}

{# CADMIN-6141 #}
{% if salt["grains.get"]("conductor:group") in ["kp-backend", "kp-prestable-backend", "kp-test-backend", "%kp-load-backend"] %}
{% set fastcgi_read_timeout = "90s" -%}
{% set fastcgi_send_timeout = "60s" -%}
{% set fastcgi_connect_timeout = "1s" -%}
{% endif -%}

{% if salt["grains.get"]("conductor:group") in ["kp-backend", "kp-prestable-backend", "kp-test-backend", "%kp-load-backend"] %}
{# CADMIN-6281 #}
map $request $phpfpm_socket {
#    ~/user  /run/php5-fpm-slow.sock;
#    ~view_random_film  /run/php5-fpm-slow.sock;
#    ~view_widget_yandex  /run/php5-fpm-slow.sock;
#    ~handler_  /run/php5-fpm-slow.sock;
#    ~/ios  /run/php5-fpm-slow.sock;
#    ~/api  /run/php5-fpm-slow.sock;
    ~gettrailer /run/php5-fpm-slow.sock;
    ~apple-app-site-association /run/php5-fpm-slow.sock;
    ~/film/ /run/php5-fpm-slow.sock;
    default /run/php5-fpm.sock;
}
{% else %}
map $request $phpfpm_socket {
    ~/ios/5.0.0/index.php\?method=getFolderContent /run/php5-fpm-slow.sock;
    default /run/php5-fpm.sock;
}
{% endif -%}


server {
{%- if yaenv == 'development' %}
    include listen_8080;

    server_name {{ servername }};
{%- else %}
    include listen;
{%- endif %}

    # fastcgi_params
    fastcgi_param PATH_INFO $path_info;
    fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    fastcgi_param QUERY_STRING $query_string;
    fastcgi_param REQUEST_METHOD $request_method;
    fastcgi_param CONTENT_TYPE $content_type;
    fastcgi_param CONTENT_LENGTH $content_length;

    fastcgi_param SCRIPT_NAME $fastcgi_script_name;
    fastcgi_param REQUEST_URI $request_uri;
    fastcgi_param DOCUMENT_URI $document_uri;
    fastcgi_param DOCUMENT_ROOT $document_root/;
    fastcgi_param SERVER_PROTOCOL $server_protocol;
    fastcgi_param REQUEST_SCHEME $scheme;
    fastcgi_param HTTPS on;

    fastcgi_param GATEWAY_INTERFACE CGI/1.1;
    fastcgi_param SERVER_SOFTWARE nginx/$nginx_version;

    fastcgi_param REMOTE_ADDR $remote_addr;
    fastcgi_param REMOTE_PORT $remote_port;
    fastcgi_param SERVER_ADDR $server_addr;
    fastcgi_param SERVER_PORT $server_port;
    fastcgi_param SERVER_NAME $server_name;

    fastcgi_param HTTP_X_FORWARDED_HOST {{ pillar.domains.kp_web }};

    fastcgi_param ANGEL $http_x_angel;

    # PHP only, required if PHP was built with --enable-force-cgi-redirect
    fastcgi_param REDIRECT_STATUS 200;

    fastcgi_param BASE_PATH /;
    fastcgi_param STICK_PATH $request_uri;

# CADMIN-8583 -- пока для девов оставляем

    include conf.d/realip.conf;

    real_ip_header X-Real-IP;

    root {{ rootdir }};

    # Add index.php to the list if you are using PHP
    index index.php; # index.phtml index_other.phtml  index.html;

    rewrite ^(/handler_[^.]*(\.php)?)$ /web/app.php$1 break;
    rewrite ^(/twitter/player)$ /web/app.php$1 break;
    rewrite ^(/(android|ipad|iphone|games/guessmovie).*)$ /web/app.php$1 break;

    location ~ \.(php|phtml)(/|$) {
            # error_log /var/log/nginx/debug.log debug;
            fastcgi_index index.php;

            # regex to split $uri to $fastcgi_script_name and $fastcgi_path
            fastcgi_split_path_info ^(.+?\.php|.+?\.phtml)(.*)$;

            # Bypass the fact that try_files resets $fastcgi_path_info
            # see: http://trac.nginx.org/nginx/ticket/321
            set $path_info $fastcgi_path_info;

            fastcgi_read_timeout {{ fastcgi_read_timeout }};
            fastcgi_send_timeout {{ fastcgi_send_timeout }};
            fastcgi_connect_timeout {{ fastcgi_connect_timeout }};

            # KPDUTY-280
            fastcgi_buffer_size 32k;
            fastcgi_buffers 4 32k;
            # Check that the PHP script exists before passing it
            try_files $fastcgi_script_name =404;

            fastcgi_pass unix:$phpfpm_socket;
    }


    # Deny access to private files.
    location ~* ^/(.*/)?\.(hg|svn|git|mc|ht.*) {
        deny all;
    }

    location / {
            try_files $uri $uri/ /index.php?$query_string;
    }

    location = /ping {
        default_type text/plain;
        return 200 'pong';
    }

}
