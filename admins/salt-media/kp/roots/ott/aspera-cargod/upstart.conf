description	"regular background program processing daemon"

start on runlevel [2345]
stop on runlevel [!2345]

respawn

setuid aspera
setgid aspera

exec /opt/aspera/cargod/bin/asperacargod -f /etc/aspera.conf -L-
