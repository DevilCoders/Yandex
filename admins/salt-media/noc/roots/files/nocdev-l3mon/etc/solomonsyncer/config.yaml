logging:
  level: info
  sinks:
    - "stderr"

solomon:
  oauth_token: '{{ pillar["robot-natasha-solomon-oauth"] }}'
  alert:
    channels:
      - id: "kitty_owtt"
        config:
          notify_about_statuses:
            - "WARN"
            - "ALARM"
