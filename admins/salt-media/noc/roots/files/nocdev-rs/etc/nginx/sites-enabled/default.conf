server {
	listen 80;
	listen [::]:80;
	listen 60889;
	listen [::]:60889;
	location / {
		header_filter_by_lua_block {
                        ngx.header['Start-Timestamp'] = ngx.req.start_time()
                }
		add_header Rs-Server $hostname;
		return 200;
	}
}

