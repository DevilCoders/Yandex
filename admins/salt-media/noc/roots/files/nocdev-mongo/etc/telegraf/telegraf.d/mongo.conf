[[inputs.mongodb]]
  servers = ["mongodb://telegraf:{{pillar['sec']['telegraf-password']}}@127.0.0.1:27017"]
  gather_perdb_stats = true
  tagexclude = ["hostname"]

  [inputs.mongodb.tags]
    _db = "ck"
