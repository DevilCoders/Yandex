/var/log/mongodb/mongod.log {
    daily
    rotate 30
    compress
    missingok
    notifempty
    sharedscripts
    postrotate
        /bin/kill -SIGUSR1 `systemctl --property=MainPID --value show  mongod` 
    endscript
}
