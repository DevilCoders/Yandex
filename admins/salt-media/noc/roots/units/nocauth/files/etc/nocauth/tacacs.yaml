logger:
  level: info
  syncs:
    - stderr

rtapi:
  endpoint: https://ro.racktables.yandex-team.ru/api
  token: "{{pillar['sec']['rtapi']}}"

postgres:
  connection-string: "user=nocauth password={{pillar['sec']['postgres']}} host=c-41aace5d-6533-416e-bc73-1bbedfda296a.rw.db.yandex.net port=6432 dbname=nocauthdb"

tacacs:
  port: 49
  secrets:
    - "{{pillar['sec']['tacacs-secrets']}}"
  password-secret: "{{pillar['sec']['tacacs-password']}}"

role-storage:
  update-period: 30s

ldap:
  host: ldap-dev.yandex.net
  port: 636
  certificate-file-path: /etc/nocauth/ldap.pem

health:
  port: 6689
