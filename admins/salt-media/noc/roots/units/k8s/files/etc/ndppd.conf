proxy {{salt['cmd.shell']('. /usr/local/sbin/autodetect_active_eth;echo $default_iface')}} {
    rule {{grains["fqdn_ip6"]|first|regex_replace('(.*:675:)[^:]*:(.*)', '\\1\\2:0/112')}} {
        static
    }
}
