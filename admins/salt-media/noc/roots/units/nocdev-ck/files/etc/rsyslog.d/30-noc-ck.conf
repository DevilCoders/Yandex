# mirror journald logs to the file
if ($programname == 'noc-ck') then {
    if ($msg contains 'DEBUG') then {
        action(type="omfile" file="/var/log/noc-ck/debug.log")
        *.*   stop
   }
   action(type="omfile" file="/var/log/noc-ck/log.log")
   *.*   stop
}
