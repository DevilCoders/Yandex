grad client rmq credentials: &grad_client_rmq_credentials
  login: "{{pillar['sec']['GRAD_CLIENT_LOGIN']}}"
  password: "{{ pillar['sec']['GRAD_CLIENT_PASSWORD'] }}"

grad server rmq credentials: &grad_server_rmq_credentials
  login: "{{ pillar['sec']['GRAD_SERVER_LOGIN'] }}"
  password: "{{ pillar['sec']['GRAD_SERVER_PASSWORD'] }}"

grad server lb credentials: &grad_server_lb_credentials
  token: "{{ pillar['sec']['LOGBROKER_TOKEN'] }}"

comocutor_credentials: &comocutor_credentials
  login: "{{ pillar['sec']['COMOCUTOR_LOGIN'] }}"
  password: "{{ pillar['sec']['COMOCUTOR_PASSWORD'] }}"

comocutor_credentials_nocauth: &comocutor_credentials_nocauth
  login: "{{ pillar['sec']['COMOCUTOR_LOGIN'] }}"
  password: "{{ pillar['sec']['COMOCUTOR_PASSWORD'] }}"
  noc_auth: true

oauth_credentials: &oauth_credentials
  token: "{{ pillar['sec']['OAUTH_TOKEN'] }}"

ekinops_credentials: &ekinops_credentials
  login: "{{ pillar['sec']['EKINOPS_LOGIN'] }}"
  password: "{{ pillar['sec']['EKINOPS_PASSWORD'] }}"

hit_credentials: &hit_credentials
  login: "root"

oauth_write_credentials: &oauth_write_credentials
  token: "{{ pillar['sec']['OAUTH_SOLOMON_PROXY'] }}"

oauth_error_booster_token:
  token: &oauth_error_booster_token "{{ pillar['sec']['ERROR_BOOSTER_TOKEN'] }}"

sevensignal_credentials: &sevensignal_credentials
  login: "{{ pillar['sec_sevensignal']['login'] }}"
  password: "{{ pillar['sec_sevensignal']['password'] }}"
  auth_key: "{{ pillar['sec_sevensignal']['auth_key'] }}"
