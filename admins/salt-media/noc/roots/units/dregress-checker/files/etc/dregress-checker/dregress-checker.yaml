logging:
  # Logging level.
  # Possible values are: "debug", "info", "warn", "error".
  level: warn
  # Where to log.
  sinks:
    - stdout
  # How to log.
  encoding: console

db_user: egress
db_password: {{pillar['sec']['password']}}
solomon_token: {{pillar['sec']['solomon_token']}}
st_token: {{pillar['sec']['st_token']}}
