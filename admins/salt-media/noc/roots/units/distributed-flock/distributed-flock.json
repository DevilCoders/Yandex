{% set zk_hosts = salt['conductor']['groups2hosts']('nocdev-zk') | map('regex_replace', '$', ':2181') | list %}
{
    "host"      :   {{ zk_hosts | json }},
    "timeout"   :   10,
    "app_id"    :   "nocdev-zk-flock"
}
