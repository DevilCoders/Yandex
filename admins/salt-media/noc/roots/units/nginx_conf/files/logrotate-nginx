/var/log/nginx/*.log /var/log/nginx*.log
{
	daily
	rotate 14
	maxsize 5G
	minsize 100M
	delaycompress
	dateext
	compress
	dateformat -%Y%m%d-%s
	notifempty
	missingok
	postrotate
		kill -USR1 `cat /var/run/nginx.pid`
	endscript
}

