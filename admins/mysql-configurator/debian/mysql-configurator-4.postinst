#!/bin/bash

set -e

# Used -c option to get users from cache.
# The main cause is autoinstall by agent, that has no access to secdist or secure repositories
/usr/bin/mysql-replace-configs-4 -i -c

# Restart configurator-4 daemons handled by ubic
/usr/bin/ubic restart mysql || true
