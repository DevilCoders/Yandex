service: nocdev
title: python_netconfig

ci:
  autocheck:
    fast-targets:
      - admins/python_netconfig

  secret: sec-01f00xrpf9sfcd6fjrdrzw8tj3

  runtime:
    sandbox-owner: NOCDEV

  releases:
    release:
      title: Release python_netconfig
      flow: release
      stages:
        build:
          title: Build python_netconfig package

  flows:
    release:
      title: Release python_netconfig package
      jobs:
        build:
          stage: build
          title: Build python_netconfig package
          description: Build python_netconfig package and upload it to dist
          task: common/arcadia/ya_package
          input:
            packages: admins/python_netconfig/debianization/debian.json
            build_type: release
            package_type: debian
            publish_package: true
            publish_to: common
            debian_distribution: unstable
            resource_type: YA_PACKAGE
            use_aapi_fuse: true
            use_arc_instead_of_aapi: false
            aapi_fallback: true
            use_new_format: true
            compress_package_archive: false
            key_user: robot-nocdeploy
