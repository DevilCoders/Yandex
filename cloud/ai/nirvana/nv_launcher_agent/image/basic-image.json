{
    "variables": {
        "token": "{{env `TOKEN`}}",
        "user": "{{env `USER`}}"
    },
    "builders": [
        {
          "type": "yandex",
          "token": "{{user `token`}}",
          "folder_id": "b1gun2gb7f017119bce0",
          "subnet_id": "e2lngl3r1nbqiijbnck5",
          "zone": "ru-central1-b",
          "labels": {
            "skip_update_ssh_keys":"true"
          },

          "image_name": "nv-launcher-agent-gpu",
          "image_family": "ubuntu-1604-ipv6-ready",
          "image_description": 
             "image for nirvana worker with nv-launcher-agent service; based on file cloud/platform/selfhost/packer-ubuntu-16.04-ipv6-ready.json",

          "source_image_family": "ubuntu-1604-lts",
          "disk_type": "network-hdd",
          "ssh_username": "ubuntu",

          "use_ipv4_nat": "false",
          "use_ipv6": "true"
        }
    ],
    "provisioners": [
        {
            "type": "shell",
            "execute_command": "chmod +x {{ .Path }}; {{ .Vars }} sudo {{ .Path }}",
            "script": "base.sh"
        },
        {
            "type": "shell",
            "script": "install.sh"
        }
    ]
}
