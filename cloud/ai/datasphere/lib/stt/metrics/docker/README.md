# Cloud ASR server package files

## Manual building

### Build separately package and resources
```
ya package docker_package.json --docker --docker-registry cr.yandex  --docker-repository crppns4pq490jrka0sth
```
