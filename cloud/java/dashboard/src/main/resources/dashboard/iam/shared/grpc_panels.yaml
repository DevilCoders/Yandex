- title: 'GRPC Errors'
  !include errors_singlestat.yaml '120%'
    params: { labels: 'sensor=grpc_statuses|servlet_statuses|http_statuses, status=@5xx' }
  params: { width: 2, height: 3 }

!include grpc_statuses_graph.yaml

!include grpc_errors_graph.yaml

!include grpc_durations_graph.yaml
