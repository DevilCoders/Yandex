type: graph
queries:
  - select: { moving_avg: [ 1m ], group_by_labels: [ sum, status_code ], alias: '{{status_code}}' }
draw:
  !include ../status_styles.yaml
yAxes: [ { min: 0 }, { min: 0 } ]
display: { empty: false, lineModes: [ bars, lines ], sort: decreasing }
