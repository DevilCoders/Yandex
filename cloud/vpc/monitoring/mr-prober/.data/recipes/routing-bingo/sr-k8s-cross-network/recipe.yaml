name: rb-sr-k8s-cross-network
description: |
  Recipe for Routing Bingo clusters with two Networks with k8s-like static routes and
  Routing Instance VM in between.
files:
- directory: files/
  recursive: true
- directory: ../common/
  exclude: ["network.tf"]
- directory: ../../common/
