name: Simple recipe with several instances
description: "Emulates recipe 'instance-group' without using Instance Groups: customizable cluster with instances in different availability zones"
files:
- directory: files/
- directory: ../common/
