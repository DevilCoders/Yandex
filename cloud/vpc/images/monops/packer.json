{
  "variables": {
    "folder_id": "{{env `FOLDER_ID`}}",
    "subnet_id": "{{env `SUBNET_ID`}}",
    "zone_id": "{{env `ZONE_ID`}}",
    "commit_author": "{{env `COMMIT_AUTHOR`}}",
    "gsid": "1000",
    "service_account_id": "{{env `SERVICE_ACCOUNT_ID`}}",
    "account_oauth_token": "{{env `YC_OAUTH_TOKEN`}}",
    "export_subnet_id": "{{env `EXPORT_SUBNET_ID`}}",
    "export_service_account_id": "{{env `EXPORT_SERVICE_ACCOUNT_ID`}}",
    "export_storage_path": "{{env `EXPORT_STORAGE_PATH`}}"
  },
  "builders": [
    {
      "type": "yandex",
      "token": "{{user `account_oauth_token`}}",
      "source_image_family": "yc-vpc-base",
      "source_image_folder_id": "{{user `folder_id`}}",

      "zone": "{{user `zone_id`}}",
      "folder_id": "{{user `folder_id`}}",
      "subnet_id": "{{user `subnet_id`}}",
      "disk_type": "network-nvme",
      "service_account_id": "{{user `service_account_id`}}",

      "labels": {
        "skip_update_ssh_keys": "true"
      },
      "metadata": {
        "gsid": "{{user `GSID`}}"
      },

      "use_ipv4_nat": true,
      "use_ipv6": true,

      "image_name": "yc-vpc-monops-{{isotime | clean_resource_name}}",
      "image_family": "yc-vpc-monops",
      "image_description": "Image for deploying monops web component",
      "image_labels": {
        "commit_author": "{{user `commit_author`}}",
        "version": "1"
      },

      "ssh_username": "ubuntu"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": [
        "sudo sed -i '/nameserver 2a02:6b8::1:1/d' /etc/resolv.conf"
      ]
    },
    {
      "type": "shell",
      "inline": [
        "echo \"Image prepared by Packer at: $(date --rfc-3339 sec)\" | sudo tee /etc/image_description",
        "echo \"Unique image ID: {{uuid}}\"       | sudo tee -a /etc/image_description",
        "echo \"Create timestamp: {{timestamp}}\" | sudo tee -a /etc/image_description"
      ]
    },
    {
      "type": "shell",
      "inline": [
        "export DEBIAN_FRONTEND=noninteractive",
        "sudo systemctl mask unattended-upgrades.service",
        "sudo systemctl stop unattended-upgrades.service",
        "while pgrep unattended; do sleep 1; done;",
        "while lsof /var/lib/dpkg/lock-frontend; do sleep 1; done;"
      ]
    },
    {
      "type": "shell",
      "inline": [
        "sudo apt-add-repository universe",
        "sudo apt-get update",
        "sudo apt-get upgrade -y --allow-unauthenticated --option=Dpkg::Options::=--force-confold"
      ]
    },
    {
      "type": "salt-masterless",
      "bootstrap_args": "stable 3004",
      "local_state_tree": "{{template_dir}}/../salt",
      "local_pillar_roots": "{{template_dir}}/../salt/pillar",
      "grains_file": "{{template_dir}}/grains",
      "log_level": "info"
    },
    {
      "type": "shell",
      "inline": [
        "echo 'Purge Salt packages'",
        "sudo apt-get -y purge salt-common salt-minion",
        "sudo rm -rf /srv/salt",
        "sudo rm -rf /srv/pillar",
        "sudo rm -rf /var/cache/salt",
        "sudo rm -rf /var/lib/cloud",
        "sudo rm -ff /var/lib/dhcp/*.leases",
        "sudo systemctl unmask unattended-upgrades.service",
        "sudo systemctl start unattended-upgrades.service",
        "echo 'List installed packages and versions'",
        "dpkg -l",
        "ls -al /var/lib/dpkg/"
      ]
    }
  ],
  "post-processors": [
   {
      "type": "yandex-export",
      "folder_id": "{{user `folder_id`}}",
      "subnet_id": "{{user `export_subnet_id`}}",
 
      "service_account_id": "{{user `export_service_account_id`}}",

      "use_ipv6": true,

      "paths": [
         "{{user `export_storage_path`}}monops/latest.qcow2",
         "{{user `export_storage_path`}}monops/{{build `ImageID`}}.qcow2"
      ],
      "keep_input_artifact": false
    },
    {
      "type": "manifest",
      "output": "{{ pwd }}/yandex-export-manifest.json",
      "strip_path": true
    }
  ]
}
