current-profile: prod
profiles:
  prod:
    # No user, means use compute.Instance.service_account for authorisation.
    environment: prod
  preprod:
    # No user, means use compute.Instance.service_account for authorisation.
    environment: preprod
  testing:
    # No user, means use compute.Instance.service_account for authorisation.
    environment: testing
  israel:
    # No user, means use compute.Instance.service_account for authorisation.
    environment: israel

