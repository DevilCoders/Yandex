[Алерт в Solomon](https://solomon.yandex-team.ru/admin/projects/yandexcloud/alerts?text=local+migration+max+time)

## Описание
Срабатывает, если время локальной миграции превысило порог в 5 сек.

## Подробности
Локальная миграция используется для обновления гипервизора. Алерт говорит о том, что на ноде какой-то инстанс слишком долго обновлялся.
При срабатывании алерта нужно остановить операцию обновления инстансов (выкатку QEMU).

Мы хотим победить ситуацию с долгими обновлениями гипервизора. Используем этот алерт для того чтобы дежурный по core получал сообщения в телеграм канал и шел на ноду искать в логе инстанс, обновление которого привело к срабатыванию алерта.

## Диагностика
- Найти в journalctl инстанс(ы) для которых был превышен порог времени миграции в 5 сек: `sudo journalctl -r | grep "Instance freeze time during migration"`
- Cобрать данные для найденных инстансов как написано в [тикете](https://st.yandex-team.ru/CLOUD-86407) и сохранить эти данные в этом же тикете
- Дальше разбираться и анализировать собранное силами команды core отдельно

## Ссылки
[Тикет на создание этого алерта](https://st.yandex-team.ru/CLOUD-83777)
[Тикет для сбора данных о долгих миграциях](https://st.yandex-team.ru/CLOUD-86407)
