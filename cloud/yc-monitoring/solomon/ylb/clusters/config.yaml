api_path: /clusters/
entities:
  # По имени строится кондукторная группа:
  #   oct  => cloud_{{env}}_oct
  #   _ALL => cloud_{{env}}
  ylb:
    template: ../../base/cluster.j2
    context:
      cloud_dns:
        israel:
          - hc-ctrl-*.svc.yandexcloud.co.il
          - hc-node-*.svc.yandexcloud.co.il
          - lb-ctrl-*.svc.yandexcloud.co.il
          - lb-node-*.svc.yandexcloud.co.il
    env_context:
      hw-cgw-dev-lab:
        no_project_id_prefix: true
      hw-cgw-ci-lab:
        no_project_id_prefix: true
