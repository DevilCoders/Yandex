# null project_id - for disable apply to another stands
{% set project_id = "yc.ydb.ydbaas-cloud" if env == "israel" else "null" %}
{% set zones_by_env = {"israel": [{"zone": "il1-a", "prefix": "m1a"}]} if env == 'israel' else {} %}
{% set default_ydb_services = [
'utils', 
'auth', 
'coordinator', 
'dsproxynode', 
'grpc', 
'interconnect', 
'kqp', 
'pdisks', 
'proxy', 
'storage_pool_stat',
'sys',
'tablets',
'vdisks',
'unified-agent',
'datastreams',
'sqs'
] %}
