{% import "vars.yaml" as vars with context %}

api_path: "/dashboards/"
entities:
  # Актуальные дэшборды
  kikimr-mt-database-overall:
    project_id: "{{ vars.project_id }}"
    menu: "MT Cluster database status"
    template: kikimr-mt-database-overall.json
  kikimr-proxy-tx-mt:
    project_id: "{{ vars.project_id }}"
    menu: "Tx Proxy Dashboard for DB"
    template: kikimr-proxy-tx-mt.json
  kikimr-device-troubleshooting:
    project_id: "{{ vars.project_id }}"
    menu: "Device Troubleshooting"
    template: kikimr-device-troubleshooting.json
  kikimr-device-2:
    project_id: "{{ vars.project_id }}"
    menu: "PDisk Device overview"
    template: kikimr-device-2.json
  distributed_storage_latencies_leveled_with_slot:
    project_id: "{{ vars.project_id }}"
    menu: "Distributed Storage Latency Overview. Multitenant Cluster. DSProxy + VDisk"
    template: distributed_storage_latencies_leveled_with_slot.json
  kikimr-proxy-tx-latency-mt:
    project_id: "{{ vars.project_id }}"
    menu: "TxProxy Latencies for DB"
    template: kikimr-proxy-tx-latency-mt.json
  kikimr-datashard-tx-mt:
    project_id: "{{ vars.project_id }}"
    menu: "DataShard Transactions for DB"
    template: kikimr-datashard-tx-mt.json
  kikimr-tablets-datashard-latency-mt:
    project_id: "{{ vars.project_id }}"
    menu: "DataShard Tablet Latency for DB"
    template: kikimr-tablets-datashard-latency-mt.json
  kikimr-datashard-tx-latency-mt:
    project_id: "{{ vars.project_id }}"
    menu: "DataShard Latency for DB"
    template: kikimr-datashard-tx-latency-mt.json
  kikimr-datashard-internals-mt:
    project_id: "{{ vars.project_id }}"
    menu: "DataShard Internals for DB"
    template: kikimr-datashard-internals-mt.json
  kikimr-coordinator-counters-mt:
    project_id: "{{ vars.project_id }}"
    menu: "Coordinator Counters for DB"
    template: kikimr-coordinator-counters-mt.json
  kikimr-coordinator-latencies-mt:
    project_id: "{{ vars.project_id }}"
    menu: "Coordinator Latencies for DB"
    template: kikimr-coordinator-latencies-mt.json
  kikimr-kqp-query-latency-mt:
    project_id: "{{ vars.project_id }}"
    menu: "KiKiMR KQP Query Latency for DB"
    template: kikimr-kqp-query-latency-mt.json
  kikimr-kqp-queries-mt:
    project_id: "{{ vars.project_id }}"
    menu: "KQP Queries for DB"
    template: kikimr-kqp-queries-mt.json
  kikimr-kqp-workers-mt:
    project_id: "{{ vars.project_id }}"
    menu: "KQP Workers for DB"
    template: kikimr-kqp-workers-mt.json
  ydb-grpc-queries:
    project_id: "{{ vars.project_id }}"
    menu: "YDB grpc Requests"
    template: ydb-grpc-queries.json

