menu: "Alerts"
menu_type: admin
api_path: /alerts/
tags: ["infra"]
entities:
  # По умолчанию не создаем алерты для HW-LAB, чтобы не тратить
  # время на их синхронизацию и не перегружать интерфейс
  # Solomon лишними алертами.
  infra_disk_repair_sla_{{ env }}:
    template: infra_disk_repair_sla.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: &hw_labs [hw-cgw-ci-lab, hw-cgw-dev-lab, hw-load-lab, hw-mellanox-lab, hw-6dxlx-lab,
                     hw1-lab, hw2-lab, hw3-lab, hw4-lab, hw6-lab, hw7-lab, hw9-lab, hw10-lab, hw11-lab]
  infra_cpu_max_frequency_{{ env }}:
    template: infra_cpu_max_frequency.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs
  infra_high_dns_resolve_time_{{ env }}:
    template: infra_high_dns_resolve_time.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs
  infra_ipv4_duplication_{{ env }}:
    template: infra_ipv4_duplication.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs
  infra_nic_temperature_{{ env }}:
    template: infra_nic_temperature.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs
  infra_nvme_disks_latency_{{ env }}:
    template: infra_nvme_disks_latency.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs
  infra_retransmit_tcp_segments_{{ env }}:
    template: infra_retransmit_tcp_segments.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs
  infra_srv_kikimr_disk_fail_{{ env }}:
    template: infra_srv_kikimr_disk_fail.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs
  infra_slab_leak_{{ env }}:
    template: infra_slab_leak.j2
    context:
      cluster: "cloud_{{ env }}"
      host_suffix: ".infra.yandexcloud.co.il"
      host_prefix: "il"
    exclude:
      env: *hw_labs

