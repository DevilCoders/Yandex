include:
  - "../base.yaml"

menu: Compute Node
submenu: "{{ env|upper }}"

objects:
  services:
    include: ./services/config.yaml
  clusters:
    include: ./clusters/config.yaml
  shards:
    include: ./shards/config.yaml

  alerts:
    include: ./alerts/config.yaml
  dashboards:
    include: ./dashboards/config.yaml
  graphs:
    include: ./graphs/config.yaml

{% if solomon == "main" %} {# MONSUPPORT-1468 ISRAEL Solomon: resolver is not initialized yet #}
  channels:
    include: ./channels/config.yaml
{% endif %}
