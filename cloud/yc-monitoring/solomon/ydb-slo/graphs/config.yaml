{% import "vars.yaml" as vars with context %}

api_path: "/graphs/"
entities:
  ydb_slo_1d_slo_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1d_slo_cluster.json
  ydb_slo_1d_slo_total_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1d_slo_total_cluster.json
  ydb_slo_1w_slo_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1w_slo_cluster.json
  ydb_slo_1w_slo_total_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1w_slo_total_cluster.json
  ydb_slo_1h_slo_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1h_slo_cluster.json
  ydb_slo_1h_slo_total_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1h_slo_total_cluster.json
  ydb_slo_1m_slo_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1m_slo_cluster.json
  ydb_slo_1m_slo_total_cluster:
    project_id: "{{ vars.project_id }}"
    template: ydb_slo_1m_slo_total_cluster.json
