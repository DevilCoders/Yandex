{% import "vars.yaml" as vars with context %}

api_path: "/dashboards/"
entities:
  # Актуальные дэшборды
  ydb_slo-ydb-availablity:
    project_id: "{{ vars.project_id }}"
    menu: "YDB Availability Dashboard"
    template: ydb_slo-ydb-availablity.json
  ydb_slo_summary:
    project_id: "{{ vars.project_id }}"
    menu: "YDB Availability Summary"
    template: ydb_slo_summary.json
