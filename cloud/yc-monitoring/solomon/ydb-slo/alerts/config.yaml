{% import "vars.yaml" as vars with context %}

api_path: /alerts/
entities:

  ydb_slo_1h_total_slo_cluster_select-1:
    template: ./ydb_slo_1h_total_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
  ydb_slo_1d_total_slo_cluster_select-1:
    template: ./ydb_slo_1d_total_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
  ydb_slo_1w_total_slo_cluster_select-1:
    template: ./ydb_slo_1w_total_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
  ydb_slo_1m_total_slo_cluster_select-1:
    template: ./ydb_slo_1m_total_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"

  ydb_slo_1h_slo_cluster_select-1:
    template: ./ydb_slo_1h_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}
  ydb_slo_1d_slo_cluster_select-1:
    template: ./ydb_slo_1d_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}
  ydb_slo_1w_slo_cluster_select-1:
    template: ./ydb_slo_1w_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}
  ydb_slo_1m_slo_cluster_select-1:
    template: ./ydb_slo_1m_slo_cluster_select-1.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}

  ydb_slo_1h_total_slo_cluster_join:
    template: ./ydb_slo_1h_total_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
  ydb_slo_1d_total_slo_cluster_join:
    template: ./ydb_slo_1d_total_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
  ydb_slo_1w_total_slo_cluster_join:
    template: ./ydb_slo_1w_total_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
  ydb_slo_1m_total_slo_cluster_join:
    template: ./ydb_slo_1m_total_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"

  ydb_slo_1h_slo_cluster_join:
    template: ./ydb_slo_1h_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}
  ydb_slo_1d_slo_cluster_join:
    template: ./ydb_slo_1d_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}
  ydb_slo_1w_slo_cluster_join:
    template: ./ydb_slo_1w_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}
  ydb_slo_1m_slo_cluster_join:
    template: ./ydb_slo_1m_slo_cluster_join.json
    project_id: "{{ vars.project_id }}"
    context:
      folder_id: "{{ vars.folder_id }}"
      notifications_channels:
      - "juggler-slo"
      channels:
      - "id": "juggler-slo"
        "config": {}
