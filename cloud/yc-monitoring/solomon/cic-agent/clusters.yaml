api_path: /clusters/
entities:
  # По имени строится кондукторная группа:
  #   oct  => cloud_{{env}}_oct
  #   _ALL => cloud_{{env}}
  cic-agent-k8s:
    template: ../base/cluster.j2
    exclude:
      solomon: [preprod, prod]
