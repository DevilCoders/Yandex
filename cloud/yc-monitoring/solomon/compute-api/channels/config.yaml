api_path: /notificationChannels/
entities:
  # Telegram-канал команды Compute API.
  # Прямое использование не приветствуется - алерты должны слаться из Juggler.
  compute_api_duty_telegram:
    template: compute_api_duty_telegram.j2
    exclude:
      solomon: [prod, preprod]

  # Ответственные за capacity: @prolog, @theigel
  compute_capacity_tg:
    template: compute_capacity_tg.j2
    exclude:
      solomon: [prod, preprod]
