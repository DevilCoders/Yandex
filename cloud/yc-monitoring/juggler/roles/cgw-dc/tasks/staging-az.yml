---
- name: 'cgw-dc-vpp-lengthy-barrier{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok) }}"
  with_items:
    - service: cgw-dc-vpp-lengthy-barrier
