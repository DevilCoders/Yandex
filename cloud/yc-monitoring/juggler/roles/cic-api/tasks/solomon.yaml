- name: 'juggler_check: error in logs'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-error-in-logs

- name: 'juggler_check: limits by JVM gc timings'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-gc-timings

# Logbroker checks
- name: 'juggler_check: limits by Logbroker MessagesRead'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-messages-read

- name: 'juggler_check: limits by Logbroker MessagesWrittenOriginal'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-messages-written

- name: 'juggler_check: limits by Logbroker PartitionMaxWriteQuotaUsage'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-partition-max-write-quota-used

- name: 'juggler_check: limits by Logbroker PartitionWriteQuotaWaitOriginal'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-partition-write-quota-wait

- name: 'juggler_check: limits by Logbroker QuotaConsumed'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-quota-consumed

- name: 'juggler_check: limits by Logbroker ReadTimeLagMs'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-read-time-lag

- name: 'juggler_check: limits by Logbroker TimeSinceLastReadMs'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-time-since-last-read

- name: 'juggler_check: limits by Logbroker TopicWriteQuotaWaitOriginal'
  juggler_check: ''
  when: env in ["prod", "preprod"]
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cic-api-logbroker-topic-write-quota-wait