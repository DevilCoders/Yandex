- name: 'juggler_check : eds-controller-leader'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: eds-controller-leader
      host: "{{ conductor_group }}_{{ dc }}"
      children:
        - "{{ conductor_group }}_{{ dc }}"
