- include: az.yml
  tags: ["lb-seed"]
  with_items: "{{ datacenters | default(omit) }}"
  loop_control:
    loop_var: dc

- include: tasks/common-meta.yml
  tags: ["lb-seed"]
- include: tasks/cleanup.yml
  tags: ["lb-seed"]
