- include: tasks/common.yml
  tags: ["local-proxy"]

- include: tasks/salt.yml
  tags: ["local-proxy"]

- include: tasks/local-lb.yml
  tags: ["local-proxy"]

- include: tasks/local-lb-solomon.yml
  tags: ["local-proxy"]

- include: tasks/local-lb-solomon-per-service.yml
  with_items: "{{ locallb_services['local-proxy'] }}"
  loop_control:
    loop_var: "locallb_service"
  var:
    locallb_service: "{{ locallb_service | default('false') | bool }}"

- include: local_proxy.yml
  tags: ["local-proxy"]

- include: tasks/cleanup.yml
  tags: ["local-proxy"]
