- include: contrail.yml
  tags: ["overlay", "compute-node-overlay"]
  with_items: "{{ datacenters|default(omit) }}"
  loop_control:
    loop_var: dc

- include: meeseeks.yml
  tags: ["overlay", "compute-node-meeseeks"]
  with_items: "{{ datacenters|default(omit) }}"
  loop_control:
    loop_var: dc

- include: e2e-tests-permnet.yml
  tags: ["overlay", "compute-node-overlay"]
  with_items: "{{ datacenters|default(omit) }}"
  loop_control:
    loop_var: dc

- include: silent.yml
  tags: ["overlay", "compute-node-overlay", "silent"]
  with_items: "{{ datacenters|default(omit) }}"
  loop_control:
    loop_var: dc
