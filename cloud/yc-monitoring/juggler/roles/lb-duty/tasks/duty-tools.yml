- name: 'hc_node_target_statuses{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits_unreach_skip) }}"
  with_items:
    - service: hc_node_target_statuses
      ttl: 560
      refresh_time: 320
