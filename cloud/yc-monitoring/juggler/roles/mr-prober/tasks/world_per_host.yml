---
- name: 'juggler_check : mr-prober'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits_nodata_skip) }}"
  with_items:
    - service: ping-external-targets
    - service: smtp-available-mx.yandex.ru
    - service: smtp-available-smtp.yandex.ru
    - service: smtp-unavailable-smtp.gmail.com
