---
- name: 'juggler_check: UNREACHABLE'
  tags: ["compute-tools", "yc-compute-api-team"]
  juggler_check: ''
  vars:
    telegram_chat_id: ComputeAPIDuty
    check_tags: ['yc-compute', 'yc-compute-api-team']
  args: "{{ silent_check | hash_merge(default_timed_limits, item) }}"
  with_items:
    - service: UNREACHABLE
      active: icmpping
      notifications: "{{ telegram_notification }}"

