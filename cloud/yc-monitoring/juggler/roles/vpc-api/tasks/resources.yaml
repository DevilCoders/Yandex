---
- name: 'juggler_check: allocator_resources-route_targets'
  juggler_check: ''
  args: "{{ allocator_resources_check | hash_merge(item) }}"
  with_items:
    - service: allocator_resources-route_targets
      children:
        - host: "solomon-alert-cloud_{{ env }}_vpc-api"
          service: allocator_resources_{{ env }}-route_targets-subnet-peering
          type: HOST
