---
- include: az.yml
  tags: ["cloudgate"]
  with_items: "{{ datacenters | default(omit) }}"
  loop_control:
    loop_var: dc
- include: tasks/cross-az.yml
  tags: ["cloudgate"]
- include: tasks/cleanup.yml
  tags: ["cloudgate"]
