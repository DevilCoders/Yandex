- name: 'cgw-daemons{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_delay_20_tag | hash_merge(item, default_timed_limits_unreach_skip_with_deployment) }}"
  with_items:
    - service: cgw-daemons
      pronounce: "Демоны у облачных врат {{ pronounce_location }}"

- name: 'cgw-daemons-related{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_delay_20_tag | hash_merge(item, default_time_limits_unreach_vpp_related_no_data_skip) }}"
  with_items:
    - service: cgw-daemons-related

- name: 'cgw-gretuns{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_delay_20_tag | hash_merge(item, default_time_limits_unreach_vpp_related) }}"
  with_items:
    - service: cgw-gretuns

- name: 'cgw-ribstats{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_delay_20_tag | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok_with_deployment) }}"
  with_items:
    - service: cgw-ribstats

- name: 'cgw-known-bugs{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_delay_20_tag | hash_merge(item, default_time_limits_unreach_vpp_related) }}"
  with_items:
    - service: cgw-known-bugs
      pronounce: "Известные ошибки гейт-вэя {{ pronounce_location }}"

- name: 'cgw-stats-fibsync{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_delay_20_tag | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok_with_deployment) }}"
  with_items:
      - service: cgw-stats-fibsync
        pronounce: "Новый фибсинк {{ pronounce_location }}"

###
# Silent checks.
- name: 'dev-run-size-check{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok) }}"
  with_items:
    - service: dev-run-size-check

- name: 'certificates-validity{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_timed_limits_unreach_skip) }}"
  with_items:
    - service: certificates-validity

- name: 'bgp2vpp-fatal-errors{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok) }}"
  with_items:
      - service: bgp2vpp-fatal-errors

- name: 'cgw_iface_traffic_rate{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok) }}"
  with_items:
    - service: cgw_iface_traffic_rate

- name: 'cgw_iface_packet_rate{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok) }}"
  with_items:
    - service: cgw_iface_packet_rate

- name: 'cgw_iface_errors{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok) }}"
  with_items:
    - service: cgw_iface_errors

- name: 'cgw-rx-miss{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok) }}"
  with_items:
    - service: cgw-rx-miss

- name: 'cgw-vpp-isolcpus{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_timed_limits_unreach_skip) }}"
  with_items:
    - service: cgw-vpp-isolcpus

- name: 'cgw-check-sniffer-off{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_timed_limits_unreach_skip) }}"
  with_items:
    - service: cgw-check-sniffer-off

- name: 'cgw-stats-ribout{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok_with_deployment) }}"
  with_items:
    - service: cgw-stats-ribout

- name: 'cgw-stats-own-announces{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok_with_deployment) }}"
  with_items:
    - service: cgw-stats-own-announces

- name: 'cgw-fibsync{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_network_limits_unreach_skip_nodata_force_ok_with_deployment) }}"
  with_items:
    - service: cgw-fibsync

- name: 'cgw-autorecovery{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check | hash_merge(item, default_timed_limits_unreach_skip_with_deployment) }}"
  with_items:
    - service: cgw-autorecovery

###
# Known bugs.
- name: 'unset-v4-vpp{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: unset-v4-vpp

- name: 'unset-v6-vpp{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: unset-v6-vpp

- name: 'arp-ipv4-v4-vpp{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: arp-ipv4-v4-vpp
      refresh_time:

- name: 'arp-ipv4-v6-vpp{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: arp-ipv4-v6-vpp

- name: 'label16-v4-vpp{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: label16-v4-vpp

- name: 'label16-v6-vpp{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: label16-v6-vpp

- name: 'cgw-fibsync-v4{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: cgw-fibsync-v4

- name: 'cgw-fibsync-v6{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ default_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: cgw-fibsync-v6

- name: 'cgw-ribout{{ dc_suffix }}'
  juggler_check: ''
  args: "{{ silent_check_kb_tag | hash_merge(item, default_timed_limits_unreach_cgw_known_bugs_skip) }}"
  with_items:
    - service: cgw-ribout
