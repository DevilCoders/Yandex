definitions:
  - &alert_id "{{.ID}}_svm_nfs_server_requests_max_time"
  - &alert_name "{{.Name}}[SVM] NFS Server Requests Max Time"
  - &alert_window_secs 600
  - &alert_annotations_tag "yc-{{.Cluster}}-nfs-solomon"
  - &alert_annotations_host "{{`{{labels.host}}`}}"
  - &alert_annotations_service "solomon_alert_nfs_server_requests_max_time"
alerts:
  - id: *alert_id
    name: *alert_name
    window_secs: *alert_window_secs
    group_by_labels:
      - host
    channels:
      - id: juggler
    annotations:
      tags: *alert_annotations_tag
      host: *alert_annotations_host
      service: *alert_annotations_service
    clusters:
      - testing_vla
      - testing_sas
      - testing_myt
      - preprod_vla
      - preprod_sas
      - preprod_myt
      - prod_vla
      - prod_sas
      - prod_myt
    expression:
      program: |-
        let ssd = {
        project='nfs',
        cluster='{{.SolomonCluster}}',
        service='server',
        request='*',
        type='ssd', sensor='MaxTime',
        host='nfs-control-{{.Zone}}*'
        };

        alarm_if(max(series_avg(ssd)) >= 5000000);
