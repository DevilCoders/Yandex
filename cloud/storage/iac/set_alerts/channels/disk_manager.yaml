channels:
  - id: telegram
    name: telegram
    method:
      telegram:
        group_title: "DM факапочная"
        text_template: ""
        send_screenshot: true

  - id: juggler
    name: juggler
    method:
      juggler:
        host: "{{`{{{annotations.host}}}`}}"
        service: "{{`{{{annotations.service}}}`}}"
        instance: ""
        description: "{{`{{annotations.juggler_description}}`}}"
        tags:
          - solomon_alert
          - "{{`{{{annotations.tags}}}`}}"
