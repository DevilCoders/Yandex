definitions:
  - defaults: &defaults
      name: "service_volume"
      port: 8770
      path: "/counters/counters=filestore/component=service_fs/spack"
      add_ts_args: false
      interval: 15
      grid_sec: 15
      protocol: "PULL_PROTOCOL_UNSPECIFIED"
      sensors_ttl_days: 1
services:
  - id: "nfs_service_volume"
    <<: *defaults
    clusters:
      - prod
    sensor_conf:
      aggr_rules:
      - cond:
        - host=*
        target:
        - host=cluster
      - cond:
        - host=vla*
        target:
        - host=cluster-vla
      - cond:
        - host=sas*
        target:
        - host=cluster-sas
      - cond:
        - host=myt*
        target:
        - host=cluster-myt
