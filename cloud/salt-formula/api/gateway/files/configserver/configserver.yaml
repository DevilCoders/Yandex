log:
  level: INFO
  add_stack_level: FATAL
  file:
    path: /var/log/configserver/error.log
    mode:
      bits: [ WRITE_USER, READ_USER, READ_GROUP, READ_ALL ]
    user: yc-api-gateway
    group: yc-api-gateway

server:
  listeners:
  - listener:
      tcp:
        port: 4435
