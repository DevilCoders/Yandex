- name: 'juggler_check : api_push_client'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits_unreach_skip) }}"
  vars:
    check_tags: "['{{ common_tag }}-{{ env }}-api-push-client']"
  with_items:
    - service: api-push-client
      active: icmpping
