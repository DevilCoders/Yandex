---
- name: 'juggler_check : cgw-daemons'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cgw-daemons

- name: 'juggler_check : cgw-announces'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cgw-announces

- name: 'juggler_check : cgw-gretuns'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cgw-gretuns

- name: 'juggler_check : cgw-ribstats'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits) }}"
  with_items:
    - service: cgw-ribstats
