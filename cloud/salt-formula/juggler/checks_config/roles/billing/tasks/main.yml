---
- include: billing.yml
  tags: ["billing"]
  vars:
    telegram_chat_id: "{{ 'YcBillingDuty' if env == 'prod' else 'YcBillingDutyPreprod'}}"
    telegram_statuses: [OK, WARN, CRIT]
    telegram_notification_delay: 60