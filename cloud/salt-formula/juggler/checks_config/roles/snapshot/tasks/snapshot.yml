---
- name: 'juggler_check: snapshot'
  vars:
    host: "yc_compute_{{ role }}_{{ env }}"
    check_tags: ['{{ common_tag }}-compute']
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, default_timed_limits_one_down_unreach_force_ok) }}"
  with_items:
    - service: snapshot