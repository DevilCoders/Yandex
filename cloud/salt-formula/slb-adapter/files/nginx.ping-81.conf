server {
    listen 81;
    listen [::]:81;
    root /run/nginx-monitor;
    location / {
        return 403;
    }
    location =/ping {
        stub_status;
    }
    location =/dns-status {
        error_page 404 /dns-status.err;
    }
    location =/dns-status.err {
        internal;
    }
}
