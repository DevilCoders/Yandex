{%- from 'opencontrail/map.jinja' import oct_db_servers -%}

{# Backup is irrelevant if it is 1 hour older then max interval between backups #}
{%- set max_backup_interval = 24 // oct_db_servers|length + 1 -%}

max_backup_age_hours: {{ max_backup_interval + 1 }}
