# Just mimic kafka config
{%- from 'opencontrail/map.jinja' import oct_db_servers -%}
zookeeper: [ {% for server in oct_db_servers %}{{ grains['cluster_map']['hosts'][server]['ipv4']['addr'] }}{% if not loop.last %},{% endif %}{% endfor %} ]
