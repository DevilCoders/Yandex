# Must be careful enough not to clash with IPVS VIPs (lo:200 up)
auto lo:2

iface lo:2 inet manual
    # Add the address contrail-named listens on
    up /bin/ip addr add 127.0.0.2/8 dev lo label $IFACE
    down /bin/ip addr del 127.0.0.2/8 dev lo label $IFACE
