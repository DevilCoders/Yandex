{%- set hostname = grains['nodename'] %}
{%- if 'loadbalancer-node' in grains['cluster_map']['hosts'][hostname]['roles'] %}
skipped_peer_groups:
  - upstream6
  - cloud_border
{%- else %}
skipped_families:
  - IPv6_MPLS
{%- endif %}
