{%- load_yaml as monitoring %}
local:
  pull-cluster-map:
    file: pull-cluster-map.sh
    type: bootstrap
  diff-cluster-map:
    file: diff-cluster-map.sh
    type: bootstrap
    execution_timeout: 120
    execution_interval: 120
{%- endload %}
