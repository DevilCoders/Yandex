{%- set config_site = {
    'livy.spark.master': 'yarn',
    'livy.spark.deploy-mode': 'cluster',
    'livy.server.session.timeout-check.skip-busy': 'true'
  }
-%}
{%- for property, value in salt['pillar.get']('data:properties:livy', {}).iteritems() -%}
    {%- do config_site.update({property: value}) -%}
{%- endfor -%}
{%- for property, value in config_site.iteritems() %}
{{ property }} = {{ value }}
{%- endfor %}
