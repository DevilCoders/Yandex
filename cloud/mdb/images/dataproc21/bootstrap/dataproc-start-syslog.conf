if ($programname == "dataproc-start") then {
        action(
                type="omfile"
                FileOwner="syslog"
                File="/var/log/yandex/dataproc-start.log"
                FileCreateMode="0644"
        )
        stop
}
