apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: external-secret
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: secret-store
    kind: SecretStore
  target:
    name: metastore-secret
  data:
  - secretKey: password
    remoteRef:
      key: fc3vme8ur18vnq2o2oda
      property: password
  - secretKey: username
    remoteRef:
      key: fc3vme8ur18vnq2o2oda
      property: username
