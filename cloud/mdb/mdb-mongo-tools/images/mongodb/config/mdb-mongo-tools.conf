
mongodb:
    port: {{conf.projects.mongodb.expose.mongod}}
    auth: True
    username: admin
    password: {{conf.projects.mongodb.users.admin.password}}
    database: {{conf.projects.mongodb.users.admin.dbname}}

control:
    service_ctl: supervisor
