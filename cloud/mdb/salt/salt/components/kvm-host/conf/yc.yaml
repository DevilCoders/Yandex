{%
    set content = {
        'default': {
            'api_url': salt['pillar.get']('data:kvm_host:template:api_url'),
            'oauth_token': salt['pillar.get']('data:kvm_host:template:oauth_token'),
            'organization': salt['pillar.get']('data:kvm_host:template:organization'),
            'project': salt['pillar.get']('data:kvm_host:template:project'),
        }
    }
%}
{{ content|yaml }}
