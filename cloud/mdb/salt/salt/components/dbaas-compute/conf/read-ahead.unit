[Unit]
Description=Set readahead for all block devices

[Service]
Type=oneshot
{% for disk in salt.grains.get('disks') %}
ExecStart=/bin/echo {{ salt.pillar.get('data:dbaas_compute:read_ahead_kb', 128) }} > /sys/block/{{ disk }}/queue/read_ahead_kb
{% endfor %}
