{% set mongodb = salt.slsutil.renderer('salt://components/mongodb/defaults.py?saltenv=' ~ saltenv) %}

domain ip6 {
    table mangle {
        chain OUTPUT {
            mod owner uid-owner '{{ mongodb.user }}' MARK set-mark 4;
        }
    }
}
