{% set data = {
    'port': mongodb.config.get(srv).net.port,
    'graceful_stop': True,
    'check_on_start': True,
    'check_log_on_start': True,
    'mongo_log_file': mongodb.config.get(srv).systemLog.path,
} %}
{% do data.update(mongodb.get('mongoctl', {})) %}
{{ data | json(indent=2) }}
