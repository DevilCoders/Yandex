{
    "host": [
{% for host in zk_hosts %}
        "{{ host }}:2181"{% if not loop.last %},{% endif %}
{% endfor %}
    ],
    "timeout": 5,
    "app_id": "{{ zk_root }}",
    "logger": {
            "path": "/var/log/mongodb/zkflock.log",
            "level": "INFO",
            "zklevel": "INFO"
    }
}
