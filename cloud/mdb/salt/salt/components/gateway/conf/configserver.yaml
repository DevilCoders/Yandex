log:
  level: DEBUG
  add_stack_level: FATAL

generate_healthchecks_and_gateway_clusters: true

server:
  listeners:
  - listener:
      tcp:
        port: 4435

front_envoy:
  hcm_options:
    no_timeouts: true
