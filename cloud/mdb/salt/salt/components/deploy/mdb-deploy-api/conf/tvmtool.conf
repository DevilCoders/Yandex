{
    "port": {{ salt['pillar.get']('data:tvmtool:port', 0) }},
    "clients": {
        "mdb-deploy-api": {
            "secret": "{{ salt['pillar.get']('data:tvmtool:config:secret', '__NOSECRET__') }}",
            "self_tvm_id": {{ salt['pillar.get']('data:tvmtool:tvm_id', 0) }},
            "dsts": {
                "blackbox": {
                    "dst_id": 223
                }
            }
        }
    }
}
