{% set root = salt.pillar.get('data:salt_master:root', '/srv') %}
[s3]
bucket = {{ salt.pillar.get('data:salt_master:s3_images_bucket', 'mdb-salt-images') }}
[master]
srv = {{ root }}
srv_images = {{ root }}-images
