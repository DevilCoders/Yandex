SHELL=/bin/sh
PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin

#*/5 * * * * root chown -R root:root /srv >/dev/null 2>&1; chmod -R g+w /srv >/dev/null 2>&1; chmod -R o+w /srv >/dev/null 2>&1
