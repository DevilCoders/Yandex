{
    "host": {{ salt.pillar.get('data:zk:hosts') | json }},
    "timeout": 5,
    "app_id": "{{ salt.pillar.get('data:zk:id') }}",
    "logger": {
        "path": "/var/log/mdb-salt-sync/zkflock.log",
        "level": "INFO",
        "zklevel": "INFO"
    }
}
