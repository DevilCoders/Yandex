checkouts_path: /salt-images/checkouts
images_dir: /salt-images
checkouts_max_age: '3h'
publish:
    keep_images: 64
    envs: {{ salt.pillar.get('data:mdb-salt-sync:publish:envs') | json(indent=4) }}

app:
    sentry:
        dsn: {{ salt.pillar.get('data:mdb-salt-sync:sentry:dsn') }}
        environment: {{ salt.pillar.get('data:sentry:environment') }}

root_repo:
    repo_uri: svn+ssh://arcadia.yandex.ru/arc/trunk/arcadia
    repo_path: /cloud/mdb/salt
    mount_path: /srv

