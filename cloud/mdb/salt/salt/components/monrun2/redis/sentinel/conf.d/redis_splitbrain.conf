[redis_splitbrain]
execution_interval=300
execution_timeout=295
command=/usr/local/yandex/monitoring/redis_splitbrain.py
type=redis
{% if salt.dbaas.is_compute() and salt.dbaas.is_dataplane() %}
send_for_host={{ salt.dbaas.managed_hostname() }}
{% endif %}
