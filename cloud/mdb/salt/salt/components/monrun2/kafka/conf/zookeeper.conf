{% for metric in ['zk_alive', 'zk_avg_latency', 'zk_min_latency', 'zk_max_latency', 'zk_queue', 'zk_descriptors', 'zk_election', 'tls'] %}
[{{ metric }}]
execution_interval=60
execution_timeout=30
command=echo '0;Not a ZK host'
type=zookeeper
{% if salt.pillar.get('data:dbaas:vtype') == 'compute' and salt.pillar.get('data:dbaas:cluster_id') %}
send_for_host={{ salt.dbaas.managed_hostname() }}
{% endif %}
{% endfor %}
