[mdb_dns_resolve_checker]
execution_interval=60
execution_timeout=45
{% if salt.dbaas.is_dataplane() %}
command=/usr/local/yandex/monitoring/mdb_dns_resolve_checker.py
{% else %}
command=/bin/echo '0; OK'
{% endif %}
type=mdb-dns-resolve
{% if salt.pillar.get('data:dbaas:vtype') == 'compute' and salt.dbaas.is_dataplane() %}
send_for_host={{ salt.dbaas.managed_hostname() }}
{% endif %}
