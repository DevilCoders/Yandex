addrs:
{% for addr in salt['pillar.get']('data:secretsstore:postgresql:hosts', {}) %}
  - {{ addr }}
{% endfor %}
db: {{ salt['pillar.get']('data:secretsstore:postgresql:db', 'dbaas_metadb') }}
user: {{ salt['pillar.get']('data:secretsstore:postgresql:user', 'mdb_health') }}
password: {{ salt['pillar.get']('data:secretsstore:postgresql:password', '') }}
sslmode: 'verify-full'
sslrootcert: '/opt/yandex/allCAs.pem'
