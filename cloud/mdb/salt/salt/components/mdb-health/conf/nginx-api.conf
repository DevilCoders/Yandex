server {
    listen 80;
    listen [::]:80;
    server_name {{ salt['pillar.get']('data:mdb-health:server_name') }} localhost;

    include /etc/nginx/includes/unistat.conf;

    location ~ ^/(ping|stat) {
        rewrite ^/(.*) /v1/$1 break;
        proxy_pass http://[::1]:12345;
    }

    location / {
        rewrite (.*)$ https://$host$request_uri permanent;
    }
}

server {
    listen 443 spdy ssl;
    listen [::]:443 spdy ssl;
    server_name {{ salt['pillar.get']('data:mdb-health:server_name') }} localhost;

    ssl_certificate /etc/nginx/ssl/mdb-health.pem;
    ssl_certificate_key /etc/nginx/ssl/mdb-health.key;

    ssl_protocols TLSv1.2;

    server_tokens off;

    ssl_prefer_server_ciphers on;
    ssl_ciphers kEECDH+AESGCM+AES128:kEECDH+AES128;

    ssl_session_cache shared:mdb-health:128m;
    ssl_session_timeout 28h;

    include /etc/nginx/includes/unistat.conf;

    location ~ ^/(ping|stat) {
        rewrite ^/(.*) /v1/$1 break;
        proxy_pass http://[::1]:12345;
    }

    location / {
        proxy_pass http://[::1]:12345;
    }
}
