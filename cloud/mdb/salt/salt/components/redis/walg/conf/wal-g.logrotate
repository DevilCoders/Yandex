{{log_dir}}/*.log {
  daily
  missingok
  rotate 7
  compress
  copytruncate
  notifempty
  maxsize 128M
  create 0644 mdb-backup mdb-backup
  postrotate
    chown -R mdb-backup:mdb-backup /var/log/wal-g/*.log
  endscript
}
