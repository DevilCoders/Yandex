{
  "redis_port": {{ salt.mdb_redis.get_redis_notls_port() }},
  "replication_port": {{ salt.mdb_redis.get_redis_replication_port() }},
  "sentinel_port": {{ salt.mdb_redis.get_sentinel_notls_port() }},
  "reset_cmd": "{{ salt.pillar.get('data:redis:secrets:sentinel_renames:RESET', 'reset') }}",
  "ha_hosts": {{ salt.pillar.get('data:dbaas:shard_hosts', []) | json }},
  "slaveof_cmd": "{{ salt.pillar.get('data:redis:secrets:renames:REPLICAOF', 'slaveof') }}",
  "config_cmd": "{{ salt.pillar.get('data:redis:secrets:renames:CONFIG', 'config') }}"
}
