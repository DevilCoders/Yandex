{% set pillar_prefix = 'data:s3db:{{ script }}:logrotate' %}
/var/log/s3/{{ script }}.log {
    {{ salt['pillar.get']('{{ pillar_prefix }}:period', 'weekly') }}
    create 0664 s3 s3
    rotate {{ salt['pillar.get']('{{ pillar_prefix }}:rotate', '12') }}
    compress
    missingok
}
