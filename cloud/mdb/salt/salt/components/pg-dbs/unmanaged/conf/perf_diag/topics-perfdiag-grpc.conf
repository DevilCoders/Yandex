{% for table in tables %}
- name: /var/log/dbaas-pg-stat-reporter/pg-stat-{{ table | replace('_', '-') }}.log
  topic: {{ salt.pillar.get('data:perf_diag:topics:pg_stat_' ~ table, 'topic') }}
{% endfor %}
