{%- if accumulator | default(False) -%}
instance="--instance-getter {{ accumulator['yasmagent-instance-getter'] | join(" --instance-getter ") }}"
{% else %}
instance="--instance-getter /usr/local/yasmagent/default_getter.py"
{% endif %}
