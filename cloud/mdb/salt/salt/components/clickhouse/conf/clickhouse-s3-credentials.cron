MAILTO=''
# Get new IAM token for s3 auth. Service account '{{ salt.pillar.get('data:service_account_id', None) }}'

{% for conf in ['0 * * * *', '@reboot'] %}
{{ conf }} root /usr/bin/ch-s3-credentials update --endpoint '{{ salt.pillar.get('data:object_storage:endpoint', None) }}' --random-sleep
{% endfor %}
