{% set users=salt['pillar.get']('data:config:pgusers', {}) %}
{% for user in users.keys() | sort %}
{% if users[user]['bouncer'] and users[user]['create'] %}
"{{ user }}" "md5{{ salt['hashutil.md5_digest'](users[user]['password'] + user) }}"
{% endif %}
{% endfor %}
