{% if misfire_grace_time is not defined %}
{% set misfire_grace_time = 10 %}
{% endif %}
{% if timeout is not defined %}
{% set timeout = 10 %}
{% endif %}
{% if schedule is not defined %}
{% set schedule = {'trigger': 'interval', 'seconds': 60} %}
{% endif %}
{% set conf = {
    'id': module,
    'module': module,
    'misfire_grace_time': misfire_grace_time,
    'timeout': timeout,
    'args': args,
    'schedule': schedule,
}
%}
{{ conf | json(indent=4) }}
