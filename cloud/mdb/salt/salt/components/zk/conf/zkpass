{% set user = 'super' %}
{% set password = salt.pillar.get('data:zk:users:super:password', None) %}
{% if not password %}
{%   set user = 'clickhouse' %}
{%   set password = salt.pillar.get('data:clickhouse:zk_users:clickhouse:password', None) %}
{% endif %}

{%
    set conf = {
        'user': user,
        'password': password,
    }
%}
{{ conf|json(indent=4) }}
