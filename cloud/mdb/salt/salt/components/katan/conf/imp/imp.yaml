log_level: Info
sentry:
  environment: {{ salt.pillar.get('data:sentry:environment') }}
  dsn: {{ salt.pillar.get('data:katan:sentry:dsn:imp') }}
metadb:
    addrs: {{ salt.pillar.get('data:metadb:hosts') }}
    db: dbaas_metadb
    user: katan_imp
    password: {{ salt.pillar.get('data:config:pgusers:katan_imp:password') | yaml_encode }}
katandb:
    addrs: {{ salt.pillar.get('data:katandb:hosts') }}
    db: katandb
    user: katan_imp
    password: {{ salt.pillar.get('data:config:pgusers:katan_imp:password') | yaml_encode }}
