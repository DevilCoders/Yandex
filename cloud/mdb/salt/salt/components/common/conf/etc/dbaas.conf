{% set data = salt['pillar.get']('data:dbaas') %}
{% if salt['pillar.get']('data:cluster_nodes', None) %}
{% do data.update({'cluster_nodes': salt['pillar.get']('data:cluster_nodes')}) %}
{% endif %}
{{ data | json(indent=4) }}
