{% from "components/postgres/pg.jinja" import pg with context %}
[sys_diskusage_by_path]
plugin = postgres
module = sys_diskusage_by_path
interval = 120
path={{ pg.data }}
alias=pgdata

yasm_ttl = 120
yasm_suffix = vmmv,tmmx
