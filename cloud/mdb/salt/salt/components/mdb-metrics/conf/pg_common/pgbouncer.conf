{% from "components/postgres/pg.jinja" import pg with context %}
[pgbouncer]
plugin = pgbouncer
module = pgbouncer
interval = 5
yasm_suffix = vmmv,tmmx
{% if pg.connection_pooler == 'odyssey' %}
show_pools_available = True
{% endif %}
