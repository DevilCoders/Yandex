[pg_sli]
plugin = pgbouncer
module = pg_sli
interval = 15
yasm_ttl = 15
yasm_suffix = vmmv,tmmx

ro_disk_consumption_limit = 0.97

databases = postgres
query_read = SELECT 1
query_write = INSERT INTO public.pg_sli_test values(1) ON CONFLICT (iid) DO UPDATE SET iid = 1 RETURNING iid;
