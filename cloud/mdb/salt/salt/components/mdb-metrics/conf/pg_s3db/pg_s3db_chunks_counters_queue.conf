[pg_s3db_chunks_counters_queue]
plugin = postgres
module = pg_query
interval = 3600

query = SELECT n_live_tup AS chunks_counters_queue_size FROM pg_stat_user_tables WHERE schemaname = 's3' AND relname = 'chunks_counters_queue';
databases = s3db

yasm_ttl = 3600
yasm_suffix = tmmx
