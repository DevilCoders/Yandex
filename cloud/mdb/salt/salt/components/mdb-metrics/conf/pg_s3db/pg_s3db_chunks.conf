[pg_s3db_chunks]
plugin = postgres
module = pg_query
interval = 3600

query = SELECT count(DISTINCT bid) AS buckets_count, count(*) AS chunks_count FROM s3.chunks WHERE bid != '00000000-0000-0000-0000-000000000000';
databases = s3db

yasm_ttl = 3600
yasm_suffix = tmmx
