[pg_s3db_maintenance_queue]
plugin = postgres
module = pg_query
interval = 1200

query = SELECT n_live_tup AS maintenance_queue_size FROM pg_stat_user_tables WHERE schemaname = 's3' AND relname = 'maintenance_queue';
databases = s3db

yasm_ttl = 1200
yasm_suffix = tmmx
