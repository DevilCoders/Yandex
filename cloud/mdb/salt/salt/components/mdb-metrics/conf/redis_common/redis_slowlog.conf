[redis_slowlog]
plugin = common
module = redis_slowlog
interval = 15

top_count = {{ salt.pillar.get('data:mdb_metrics:redis_slowlog:top_count', 5) }}
info_file = {{ salt.mdb_metrics.get_redis_shared_metrics() }}
