[mongodb_mongos_sharding_health]
plugin = common
module = mongodb_sharding
interval = {{ 15 if not is_burst else (15, min_burst_interval) | max}}
metrics = balancer-enabled, balancer-in-round, active-migrations

conn_pool_name = mongos

yasm_suffix = vmmv,tmmx
yasm_tags_db = mongos
yasm_ttl = 300
