[zk_sli]
plugin = common
module = zk_sli
interval = {{ 15 if not is_burst else (15, min_burst_interval) | max }}

host = 127.0.0.1
port = 2181

root_node = /sli_check
read_node = read_sli_common
write_node = write_sli_{{ salt.grains.get('id') }}
