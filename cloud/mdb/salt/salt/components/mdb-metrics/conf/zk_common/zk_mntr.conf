[zk_mntr]
plugin = common
module = zk_mntr
interval = {{ 5 if not is_burst else (5, min_burst_interval) | max }}

yasm_suffix = vmmv,tmmx
yasm_tags_db = zookeeper

timeout = 3
host = 127.0.0.1
port = 2181
