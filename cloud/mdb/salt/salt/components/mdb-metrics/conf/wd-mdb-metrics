PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
*/5 * * * * root sleep $((RANDOM \% 120)); /usr/local/yandex/monitoring/mdb_metrics_senders.py 2>/dev/null | grep -q ^0 || (service mdb-metrics restart; logger -p daemon.err "mdb-metrics restarted") >/dev/null 2>&1
