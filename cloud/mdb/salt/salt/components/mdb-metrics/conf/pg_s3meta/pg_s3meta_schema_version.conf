[pg_s3meta_schema_version]
plugin = postgres
module = pg_query
interval = 3600

query = SELECT max(version) AS schema_version FROM schema_version;
databases = s3meta

yasm_ttl = 7200
yasm_suffix = xxxv
