[pg_s3meta_move_queue]
plugin = postgres
module = pg_query
interval = 1200

query = SELECT count(*) AS move_queue_size FROM s3.chunks_move_queue;
databases = s3meta

yasm_ttl = 1200
yasm_suffix = tmmx
