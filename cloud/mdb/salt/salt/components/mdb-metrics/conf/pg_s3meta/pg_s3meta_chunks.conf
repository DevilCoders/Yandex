[pg_s3meta_chunks]
plugin = postgres
module = pg_query
interval = 3600

query = SELECT count(distinct(bid)) AS buckets_count, count(1) AS chunks_count FROM s3.chunks;
databases = s3meta

yasm_ttl = 3600
yasm_suffix = vmmv
