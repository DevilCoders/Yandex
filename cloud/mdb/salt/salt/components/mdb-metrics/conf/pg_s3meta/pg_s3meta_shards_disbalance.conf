[pg_s3meta_shards_disbalance]
plugin = postgres
module = pg_query
interval = 3600

query = SELECT -round(sum(diff)) AS shard_disbalance FROM s3.shard_disbalance_stat WHERE diff < 0;
databases = s3meta

yasm_ttl = 3600
yasm_suffix = tmmx
