{% from "components/greenplum/map.jinja" import gpdbvars with context %}

[main]
log_path = /var/log/greenplum/s3-backup.log
timeout = {{ salt['pillar.get']('data:walg:restore_point_timeout', 1800) }}
gp_admin = {{ gpdbvars.gpadmin }}
