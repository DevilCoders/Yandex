{% for scids,info in salt['pillar.get']('data:dbaas:cluster:subclusters', {}).items() %}
{%   if info['name'] == 'segment_subcluster' %}
{%     for host in info['hosts']|sort %}
{{ host }}
{%     endfor %}
{%   endif %}
{% endfor %}
