{%- from "components/greenplum/map.jinja" import gpdbvars with context -%}
# hostname:port:database:username:password
*:*:*:{{ gpdbvars.gpadmin }}:{{ salt['pillar.get']('data:restore-from-pillar-data:greenplum:users:gpadmin:password', '') }}
