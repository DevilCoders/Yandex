{% from "components/greenplum/map.jinja" import gpdbvars with context %}
{% set cron_minutes_interval = salt['pillar.get']('data:walg:restore_point_interval', 60)|int %}
{% if cron_minutes_interval > 0 %}
PATH=/bin:/sbin:/usr/bin:/usr/sbin:/usr/local/bin:/usr/local/sbin
MAILTO=''

*/{{ cron_minutes_interval }} * * * * {{ gpdbvars.gpadmin }} flock -w 10 -o /tmp/gp_walg_create_restore_point.lock /usr/local/yandex/gp_walg_create_restore_point.py
{% endif %}
