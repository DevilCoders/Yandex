pgaas_unreach_pass:
  aggregator_kwargs:
    unreach_mode: force_ok
    unreach_service:
      - check: "{{ unreach_cgroup | default('') }}:META"
      - check: "{{ unreach_cgroup | default('') }}:UNREACHABLE"
      - check: :health_check
      - check: :dead_bouncers
