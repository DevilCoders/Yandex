es_checks:
  host: '{{ host }}'
  notifications: "{{ notifications }}"
  aggregator: logic_or
  aggregator_kwargs:
      mode: WARN
  namespace: "{{ namespace }}"
  __force__: True
  children:
    type: CGROUP
    name: '{{ cgroup }}'
