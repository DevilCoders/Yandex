- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    namespace: dbaas.postgresql
    host: postgresql-backup-service-compute-prod
    cgroup: mdb_backup_compute_prod
    ctype: postgresql_cluster
    installation: compute_prod

  tasks:
    - juggler_facts: jserver_api=http://juggler-api.search.yandex.net:8998/api
    - include_vars: ../configs/gendb_resps.yml
    - include_vars: ../configs/common.yml
    - include_vars: ../configs/notifications_nocalls.yml
    - include: ../checks/backup-service-per-ctype.yml
  post_tasks:
    - juggler_cleanup: jcheck_mark={{ host }}
