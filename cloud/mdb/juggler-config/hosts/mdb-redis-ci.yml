- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    namespace: dbaas.redis
    host: mdb-redis-ci

  tasks:
    - juggler_facts: jserver_api=http://juggler-api.search.yandex.net:8998/api
    - include_vars: ../configs/nonrelational_dbs_resps.yml
    - include_vars: ../configs/notifications_nocalls.yml

    - juggler_check:
        host: "{{ host }}"
        service: redis-master-ci
        namespace: "{{ namespace }}"
        aggregator: logic_or
        aggregator_kwargs:
          mode: 'WARN'
        notifications: "{{ notifications }}"
        refresh_time: 600
        ttl: 3600
        children:
            host: ci01k.db.yandex.net
            service: redis_master_ci
        __force__: True

  post_tasks:
    - juggler_cleanup: jcheck_mark={{ host }}
