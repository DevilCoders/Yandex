- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    namespace: dbaas.greenplum
    host: greenplum-worker-porto-test
    cgroup: mdb_worker_porto_test

  tasks:
    - juggler_facts: jserver_api=http://juggler-api.search.yandex.net:8998/api
    - include_vars: ../configs/gendb_resps.yml
    - include_vars: ../configs/notifications_nocalls.yml
    - include: ../checks/dbaas-worker-greenplum.yml
  post_tasks:
    - juggler_cleanup: jcheck_mark={{ host }}
