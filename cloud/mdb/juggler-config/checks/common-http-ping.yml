- juggler_check:
  args: "{{ common_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: http-ping
      refresh_time: 53
      flap:
        stable: 60
        critical: 180
      children:
        service: http_ping
