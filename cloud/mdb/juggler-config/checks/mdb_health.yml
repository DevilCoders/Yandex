- juggler_check:
    host: "{{ host }}"
    namespace: "{{ namespace }}"
    tags: "{{ default_tags | default([]) }}"
    service: mdb-health-clients
    aggregator: logic_or
    refresh_time: 60
    children:
      service: clients_not_reduce

- juggler_check:
    host: "{{ host }}"
    namespace: "{{ namespace }}"
    tags: "{{ default_tags | default([]) }}"
    aggregator: logic_or
    service: mdb-health-aggregate-time
    refresh_time: 30
    children:
      service: aggregate_time
