- juggler_check:
  args: "{{ es_workhours_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: kibana-ping
      flap:
        stable: 180
        critical: 240
      refresh_time: 60
      children:
        service: kibana_ping
