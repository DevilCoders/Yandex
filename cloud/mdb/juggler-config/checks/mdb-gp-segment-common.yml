- juggler_check:
  args: "{{ gp_common_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: gp-segment-pxf
      refresh_time: 60
      ttl: 300
      children:
        service: gp_segment_pxf
