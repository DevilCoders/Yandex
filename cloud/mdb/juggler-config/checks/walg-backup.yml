- juggler_check:
  args: "{{ workhours_logic_or | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: walg-backup-check
      refresh_time: 8192
      ttl: 86400
      children:
        service: walg_backup_check
