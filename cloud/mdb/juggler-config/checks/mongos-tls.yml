- juggler_check:
  args: "{{ mongos_common_checks | hash_merge( mongos_unreach_pass, item ) }}"
  with_items:
    - service: tls
      refresh_time: 3600
      tags:
        - "mdb.{{ installation }}"
      ttl: 14400
      children:
        service: tls
