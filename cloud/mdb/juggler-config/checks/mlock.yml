- juggler_check:
  args: "{{ common_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: mlock-stale
      refresh_time: 180
      ttl: 1800
      children:
        service: mlock_stale
