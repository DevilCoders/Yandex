- juggler_check:
  args: "{{ pg_backup_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: mongodb-backup
      refresh_time: 300
      children:
        service: mongodb_backup_health
