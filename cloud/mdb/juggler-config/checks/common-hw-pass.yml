- juggler_check:
  args: "{{ common_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: if-errors
      refresh_time: 300
      children:
        service: if_errors

- juggler_check:
  args: "{{ common_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: raid
      refresh_time: 300
      children:
        service: linux_raid

- juggler_check:
  args: "{{ common_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: load-average
      refresh_time: 300
      children:
        service: load_average
