- juggler_check:
    host: "{{ host }}"
    service: grpc-ping
    namespace: "{{ namespace }}"
    aggregator: logic_or
    refresh_time: 60
    ttl: 900
    children:
        name: "{{ cgroup }}"
        type: "CGROUP"
        service: grpc_ping
    __force__: True
