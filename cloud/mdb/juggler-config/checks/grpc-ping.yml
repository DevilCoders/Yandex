- juggler_check:
  args: "{{ common_checks | hash_merge( unreach_pass, item ) }}"
  with_items:
    - service: grpc-ping
      refresh_time: 60
      ttl: 900
      children:
        service: grpc_ping
