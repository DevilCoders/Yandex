{
    "id": "<< env >>-hasclients",
    "projectId": "{{projectId}}",
    "name": "[<< svc >>][<< env >>] mdb-health client count should not reduce a lot",
    "version": 1,
    "createdBy": "isiv",
    "notificationChannels": ["<< notification_channel >>"],
    "type": {
        "expression": {
            "program": "{{alert_program}}",
            "checkExpression": "is_red"
        }
    },
    "annotations": {
        "trafficLight.color": "{{expression.trafficColor}}",
        "host": "<< health_cluster_name >>_vhost",
        "service": "clients_not_reduce",
        "description": "mdb-health client count: {{expression.avg_clients_by_dc}}, average diff change: {{expression.avg_diff_clients}}"
    },

    "periodMillis": 900000, "delaySeconds": 60,
    "description": "When goroutines or threads more than limit"
}
