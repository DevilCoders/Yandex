{
  "id": "<< id_prefix >>-dbm-gen3-cpu",
  "projectId": "<< project_id >>",
  "name": "<< human_prefix >>-dbm-gen3-cpu",
  "notificationChannels": [
    "<< notification_channel_tagged >>"
  ],
  "type": {
    "expression": {
      "program": "{% include "alerts/dbm/dbm-gen3-flavors-cpu" %}"
    }
  },
  "annotations": {
    "description": "Allocation of gen3 vCPU: {% for dc in dc_lst_gen3 %} <<dc>> - {{expression.<<dc>>_last}} {%- if not loop.last %},{% endif %}{% endfor %}",
    "value": "{{expression.result}}",
    "host": "mdb.yandex-team.ru",
    "service": "free-resources-g3-cpu"
  },
  "periodMillis": 300000,
  "delaySecs": 120,
  "description": ""
}
