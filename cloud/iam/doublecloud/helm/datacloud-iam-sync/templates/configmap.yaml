apiVersion: v1
kind: ConfigMap
metadata:
  name: "iam-sync-configs"
  namespace: "iam"
data:
  {{- with $.Values.endpoints }}
  endpoints.yaml: |
    endpoints:
      {{- toYaml . | nindent 6 }}
  {{- end }}
