apiVersion: v1
kind: Secret
metadata:
  name: "populate-db-secrets"
  namespace: "iam"
type: Opaque
data:
{{- range $k, $v := .Values.secrets }}
  {{ $k }}: {{ $v | b64enc }}
{{- end }}
{{- range $k, $v := .Values.secretsBase64 }}
  {{ $k }}: {{ $v }}
{{- end }}