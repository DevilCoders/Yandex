apiVersion: v1
kind: ConfigMap
metadata:
  name: "populate-db-configs"
  namespace: "iam"
data:
  {{- $configFiles := printf "%s/populate-db/files/{config.toml,ids.yaml}" $.Values.cloudServiceProvider }}
{{ (.Files.Glob $configFiles).AsConfig | indent 2 }}
