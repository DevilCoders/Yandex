apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: iam-ca-issuer
  namespace: "iam"
  annotations:
    helm.sh/hook-weight: "-100500"
spec:
  ca:
    secretName: "cert-manager-iam-ca-key-pair"
