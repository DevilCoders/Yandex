Configuration:
  Appenders:
    Console:
      name: stdout
      PatternLayout:
        Pattern: "%d %-5level %-6X{tx} [%t] %c{1.}: %msg%n%throwable"

  Loggers:
    Root:
      AppenderRef:
        - ref: stdout
