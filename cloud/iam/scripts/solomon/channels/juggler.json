{
  "id": "juggler",
  "projectId": "${PROJECT_ID}",
  "name": "juggler",
  "notifyAboutStatuses": [
    "OK",
    "ERROR",
    "ALARM"
  ],
  "method": {
    "juggler": {
      "host": "yc_iam_solomon_check_${ENV_ID}",
      "service": "{{alert.projectId}}_{{alert.id}}",
      "instance": "",
      "description": "{{alert.id}} is {{status.code}} since {{since}}\n{{annotations.description}}\n{{{annotations.url}}}",
      "tags": [
        "iam",
        "${ENV_ID}_iam",
        "solomon"
      ]
    }
  }
}
