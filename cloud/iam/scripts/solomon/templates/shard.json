{
  "projectId": "${PROJECT_ID}",
  "id": "${PROJECT_ID}_${CLUSTER_ID}_${SERVICE_NAME}",
  "clusterId": "${PROJECT_ID}_${CLUSTER_ID}",
  "serviceId": "${PROJECT_ID}_${SERVICE_NAME}",
  "state": "RW"
}
