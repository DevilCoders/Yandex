{
  "id": "${CLUSTER_NAME}-immutable-cache-age",
  "projectId": "${PROJECT_ID}",
  "name": "${CLUSTER_NAME} immutable cache age",
  "type": {
    "expression": {
      "program": "let maxAge = series_max({project='${PROJECT_ID}', cluster='${CLUSTER_NAME}', service='${SERVICE_NAME}', host='iam-*', sensor='guava_cache_age_seconds', cache='PermissionEntityCache|EncryptionKeyCache'});",
      "checkExpression": "max(maxAge) > 3630"
    }
  },
  "annotations": {
    "description": "Status not equal to OK means some failures during cache update.",
    "url": "https://${SOLOMON_ENDPOINT}/admin/projects/${PROJECT_ID}/alerts/${CLUSTER_NAME}-immutable-cache-age"
  },
  "windowSecs": 300,
  "delaySecs": 0,
  "channels": ${CHANNELS}
}
