{
  "id": "${PROJECT_ID}_${CLUSTER_ID}",
  "name": "${CLUSTER_NAME}",
  "projectId": "${PROJECT_ID}",
  "conductorGroups": ${CONDUCTOR_GROUPS},
  "cloudDns": ${CLOUD_DNS},
  "useFqdn": ${CLUSTER_USE_FQDN},
  "tvmDestId": ""
}
