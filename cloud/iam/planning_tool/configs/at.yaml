mds:
  bucket: 'at'
  access_key_id: SOiRust6USN7N0nez2Ov
  secret_key_file: mds_key

auth:
  token_file: token

worklog:
  day_start: '15:00:00'
  day_length: '8h'
  sp_time: '4h'
  timezone: 'Europe/Moscow'
  time_spent: 'storyPoints'

timesheet:
  board_id: 8332
  team:
    - djant
    - ivanrudn
    - zhumash
    - pjalybin
    - sergeyantonov
  in_progress_filter: 'Type: !Epic AND Queue: !GOALZ AND Queue: !OFFICEMODE'
  not_in_progress_statuses:
    - Closed
    - Open
    - New
    - Backlog
  categories:
    - name: Expansion
      condition: "'yc-at-expansion' in issue.tags"
      chart: False
    - name: Features
      condition: "'yc-at-features' in issue.tags"
      chart: False
    - name: Sustain
      condition: "'yc-at-sustain' in issue.tags"
      chart: False
    - name: Maintenance
      condition: "'yc-at-maintenance' in issue.tags"
      chart: False
    - name: Education
      condition: "'yc-at-education' in issue.tags"
      chart: False

reports:
  timesheet:
    mds:
      path: 'timesheet'

  daily:
    mds:
      path: 'daily'

  monthly:
    mds:
      path: 'monthly'
    additional_fields:
      - key
      - tags
      - type
      - queue

  tasks:
    mds:
      path: 'tasks'
