#!/usr/bin/env bash
name='queue'
check=$(sudo docker ps | grep marketplace-worker | wc -l)

if [[ ${check} -eq 1 ]] ; then
    echo "PASSIVE-CHECK:$name;0;Ok - Queue docker container is running"
else
    echo "PASSIVE-CHECK:$name;2;Down" ; exit
fi