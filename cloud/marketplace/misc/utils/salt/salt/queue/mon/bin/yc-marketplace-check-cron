#!/usr/bin/env bash
name='queue-cron'
check=$(sudo journalctl --since @$(date --date "-1min" +"%s") | grep CRON | grep yc-marketplace-check-tasks | wc -l)

if (( ${check} > 4 )) ; then
    echo "PASSIVE-CHECK:$name;0;Ok - Queue cron is working"
else
    echo "PASSIVE-CHECK:$name;2;Down" ; exit
fi
