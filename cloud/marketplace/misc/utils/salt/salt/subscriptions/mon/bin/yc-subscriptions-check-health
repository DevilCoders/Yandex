#!/bin/bash
name='subscriptions'
health_url="http://localhost:9894/readiness"
check=$(/usr/bin/curl -fs ${health_url})

if [[ ${check} ]] ; then
    echo ${check}
else
    echo "PASSIVE-CHECK:$name;2;Down" ; exit
fi
