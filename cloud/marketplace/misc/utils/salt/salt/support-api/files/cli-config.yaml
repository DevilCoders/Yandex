e2e: {{ pillar['support-e2e'] | yaml(true) }}
endpoints:
  support:
    url: "http://localhost:5001"
  st:
    url: {{ pillar['support-api']['st']['url']}}
  identity:
    url: "{{ pillar['api']['iam_api'] }}"
st:
  oauth: "{{ pillar['support-secret']['st']['oauth'] }}"
folder_id: "{{ pillar['support-api']['e2e']['folder_id']}}"
cloud_id: "{{ pillar['support-api']['e2e']['cloud_id']}}"
billing_account_id: "{{ pillar['support-api']['e2e']['billing_account_id']}}"