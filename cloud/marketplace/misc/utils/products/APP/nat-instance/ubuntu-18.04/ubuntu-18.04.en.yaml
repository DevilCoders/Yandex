name: Ubuntu 18.04 LTS
consoleDescription: "Image with pre-configured routing and IP address translation rules."
siteDescription: "Image with pre-configured routing and IP address translation rules. Can be used to configure internet connections for multiple virtual machines via a single dedicated virtual machine — the NAT instance. In this case, only one public IP address will be used: the one that will be assigned to the NAT instance."
useCases: |
  * Internet connection for Kubernetes node groups.
  * Internet connection for resources without a public IP address and the guarantee that the resources themselves are not accessible from the internet.

documentation: |
  * [Routing through a NAT instance](https://cloud.yandex.com/docs/solutions/routing/nat-instance)
additionalInfo: ""
