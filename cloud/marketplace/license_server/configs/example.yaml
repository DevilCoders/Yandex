grpc:
  listen-endpoint: localhost:4444
billing-client:
  endpoint: endpoint
  retry_count: 1
ydb:
  endpoint: localhost:2135
  db-root: license_server
  database: local
  ca-path: dev/ydb/ydb_ca.pem
logger:
  level: debug
  file-paths:
    - stdout
  enable-journald: false
prefix: pre
