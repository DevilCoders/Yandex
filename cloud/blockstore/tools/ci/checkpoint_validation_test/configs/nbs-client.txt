ClientConfig {
  Host: "{{ host }}"
  Port: {{ port }}
  {%- if securePort is not none %}
  SecurePort: {{ securePort }}
  {%- endif %}
}
{%- if tokenPath is not none %}
AuthConfig {
  IamTokenFile: "{{ tokenPath }}"
}
{%- endif %}


