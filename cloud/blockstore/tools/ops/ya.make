OWNER(g:cloud-nbs)

RECURSE(
    add_agents
    adjust-channels-count
    alter-volumes
    compact-localdb
    generate-agents
    generate-packages
    kill-tablets
    pcompact-tablets
    prune-garbage
    prune-garbage-bulk
    rebuild-metadata
    remove-stale-entities
    update_limits
    write-disk-status
)
