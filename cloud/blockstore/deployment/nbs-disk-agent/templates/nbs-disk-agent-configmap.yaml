apiVersion: v1
kind: ConfigMap
metadata:
  name: nbs-disk-agent-configmap
  namespace: nbs-disk-agent
data:
  {{- range $key, $value := .Values.config.nbs-disk-agent }}
    {{- $key | nindent 2 }}: |-
    {{- $value | nindent 4 }}
  {{- end }}
