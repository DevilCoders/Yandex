apiVersion: v1
kind: ConfigMap
metadata:
  name: nbs
  namespace: nbs
data:
  {{- range $key, $value := .Values.config.nbs }}
    {{- $key | nindent 2 }}: |-
    {{- $value | nindent 4 }}
  {{- end }}
