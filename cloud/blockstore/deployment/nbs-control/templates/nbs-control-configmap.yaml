apiVersion: v1
kind: ConfigMap
metadata:
  name: nbs-control
  namespace: nbs-control
data:
  {{- range $key, $value := .Values.config.nbs_control }}
    {{- $key | nindent 2 }}: |-
    {{- $value | nindent 4 }}
  {{- end }}
