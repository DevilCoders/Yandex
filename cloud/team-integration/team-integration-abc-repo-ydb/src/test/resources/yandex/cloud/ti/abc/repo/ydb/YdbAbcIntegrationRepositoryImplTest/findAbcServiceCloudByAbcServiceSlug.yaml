- |-
  DECLARE $abc_slug AS 'Utf8';
  SELECT `abc_slug`, `cloud_id`, `default_folder_id`, `abc_id`, `abc_folder_id`, `created_at`
  FROM `team_integration/abc_services_slug_index`
  WHERE `abc_slug` = $abc_slug
