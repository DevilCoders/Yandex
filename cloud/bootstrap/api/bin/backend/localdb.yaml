port: 23712
production: False
db: # configuration of local database, raised via docker container
    host: localhost
    port: 12000
    dbname: postgres
    user: postgres
    password: postgres
    sslmode: disable  # easier to disable ssl than configure
logging:
    debug: True
auth:
    enabled: False
