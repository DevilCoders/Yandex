---

- name: Increase history size and add timestamps
  copy: src=history.sh dest=/etc/profile.d/history.sh mode=0755
  tags:
    - history

- name: Remove old history extension
  file: path=/etc/profile.d/history state=absent
  tags:
    - history
