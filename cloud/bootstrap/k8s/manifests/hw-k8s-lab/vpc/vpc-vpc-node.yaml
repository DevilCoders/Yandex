apiVersion: datadoghq.com/v1alpha1
kind: ExtendedDaemonSet
metadata:
  name: vpc-vpc-node
  namespace: vpc
spec:
  strategy:
    canary:
      replicas: 1
  template:
    metadata:
      labels:
        name: vpc-vpc-node
    spec:
      nodeSelector:
        compute-node: "1"
      hostNetwork: true
      initContainers:
      - name: vpc-vpc-node-init
        image: cr.yandex/yc-bootstrap/ubuntu:xenial-20210114-2.0
        command: ['bash', '-c', 'sleep 5; exit 0']
      containers:
      - name: vpc-vpc-node
        image: cr.yandex/yc-bootstrap/ubuntu:xenial-20210114-2.0
        command: ['bash', '-c', 'while [ 1 ]; do echo "Dummy vpc vpc-node is running"; date; sleep 10; done']
