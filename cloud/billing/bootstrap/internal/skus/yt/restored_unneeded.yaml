service: yt
skus:
  yt.bohr.compute.usage.memory.old:
    ru: Bohr. Оперативная память (Устарело)
    en: Bohr. Memory (Old)
    reporting_service: yt/bohr
    pricing_formula: mul(usage.quantity, tags.allocated_resources.user_memory)
    units:
      usage: byte*second
      pricing: gbyte*hour
    schemas:
      - yt.scheduler.pools.compute.v1
    resolving_policy: "`false`"
  yt.landau.compute.usage.memory.old:
    ru: Landau. Оперативная память (Устарело)
    en: Landau. Memory (Old)
    reporting_service: yt/landau
    pricing_formula: mul(usage.quantity, tags.allocated_resources.user_memory)
    units:
      usage: byte*second
      pricing: gbyte*hour
    schemas:
      - yt.scheduler.pools.compute.v1
    resolving_policy: "`false`"
