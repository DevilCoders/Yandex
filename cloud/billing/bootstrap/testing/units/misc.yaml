- src_unit: point
  dst_unit: 1m*points
  factor: 1000000

- src_unit: request
  dst_unit: 1m*request
  factor: 1000000

- src_unit: request
  dst_unit: 10k*request
  factor: 10000

- src_unit: request
  dst_unit: 1k*request
  factor: 1000

- src_unit: request_unit
  dst_unit: 1m*request_unit
  factor: 1000000

- src_unit: rcu*second
  dst_unit: rcu*hour
  factor: 3600

- src_unit: invocation
  dst_unit: 1m*invocation
  factor: 1000000

- src_unit: image
  dst_unit: 1k*image
  factor: 1000

- src_unit: symbol
  dst_unit: 1m*symbols
  factor: 1000000

- src_unit: key_version*second
  dst_unit: key_version*hour
  factor: 3600

- src_unit: unit*millisecond
  dst_unit: unit*second
  factor: 1000

- src_unit: interconnect*second
  dst_unit: interconnect*hour
  factor: 3600

- src_unit: interconnect*second
  dst_unit: interconnect*month
  factor: 2592000

- src_unit: model*second
  dst_unit: model*month
  factor: 2592000 # 86400 * 30

- src_unit: zone*second
  dst_unit: zone*hour
  factor: 3600

- src_unit: channel*second
  dst_unit: channel*month
  factor: 2592000 # 3600 * 24 * 30
