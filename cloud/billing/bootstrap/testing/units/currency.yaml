- src_unit: rub
  dst_unit: usd
  factor: 1 # * currency multiplier

- src_unit: usd
  dst_unit: rub
  factor: 1 # * currency multiplier

- src_unit: kzt
  dst_unit: rub
  factor: 1 # * currency multiplier

- src_unit: rub
  dst_unit: kzt
  factor: 1 # * currency multiplier

# TODO: Add kzt -> usd, usd -> kzt?

- src_unit: kop
  dst_unit: rub
  factor: 100

- src_unit: nanorub
  dst_unit: rub
  factor: 1000000000

- src_unit: cent
  dst_unit: usd
  factor: 100

- src_unit: tiyn
  dst_unit: kzt
  factor: 100

- src_unit: kop
  dst_unit: usd
  factor: 100 # (kop -> rub) * currency multiplier

- src_unit: kop
  dst_unit: kzt
  factor: 100 # (kop -> rub) * currency multiplier

- src_unit: cent
  dst_unit: rub
  factor: 100 # (cent -> usd) * currency multiplier

- src_unit: tiyn
  dst_unit: rub
  factor: 100 # (tiyn -> kzt) * currency multiplier
