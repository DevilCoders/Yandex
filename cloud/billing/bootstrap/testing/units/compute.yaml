- src_unit: instance*second
  dst_unit: instance*hour
  factor: 3600

- src_unit: fip*second
  dst_unit: fip*hour
  factor: 3600

- src_unit: core*second
  dst_unit: core*hour
  factor: 3600

- src_unit: core*millisecond
  dst_unit: core*hour
  factor: 3600000

- src_unit: gpu*second
  dst_unit: gpu*hour
  factor: 3600

- src_unit: core*hour
  dst_unit: core*month
  factor: 720

- src_unit: millivcpu*hour
  dst_unit: core*month
  factor: 720000 # 24 * 30 * 1000

- src_unit: microvcpu*second
  dst_unit: core*hour
  factor: 3600000000 # 3600 * 1000000

- src_unit: gpu*hour
  dst_unit: gpu*month
  factor: 720 # 24 * 30
