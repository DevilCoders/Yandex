$result = (
    SELECT
        service_instance_id as id,
        billing_account_id,
        effective_time,
        created_at,
        created_at as updated_at,
        "UTC" as timezone
    FROM
        $src
    WHERE service_instance_type = "cloud"
);
