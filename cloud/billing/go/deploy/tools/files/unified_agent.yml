status:
  port: "16241"
  host: null

agent_log:
  priority: "NOTICE"

storages:
  - name: main
    plugin: fs
    config:
      directory: /data/unified_agent/main
      max_partition_size: 100mb
      max_segment_size: 10mb

import:
  - /config/unified-agent-conf.d/*.yml
  - /config/unified-agent-conf.d/*.yaml
  - /unified-agent-conf.d/*.yml
  - /unified-agent-conf.d/*.yaml
