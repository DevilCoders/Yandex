version: '2.4'

services:
  terragrunt:
    image: cr.yandex/crphukr1a3brf0obl2oh/yc-billing-terraform:0.2
    network_mode: host
    environment:
      USER: ${USER}
      SKM_TOKEN: ${SKM_TOKEN}
    volumes:
      - ..:/terraform
      - ~/.ssh:/root/.ssh
