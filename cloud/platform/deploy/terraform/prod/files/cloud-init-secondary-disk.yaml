#cloud-config
mounts:
 - [ /dev/disk/by-id/virtio-home-dirs, /data, auto, "defaults", "0", "0" ]
 - [ /data/home,                       /home, none, "bind",     "0", "0" ]

fs_setup:
- filesystem: ext4
  device: /dev/disk/by-id/virtio-home-dirs
  partition: auto
  overwrite: false
