# install yc
'curl -s https://storage.yandexcloud.net/yandexcloud-yc/install.sh | bash -s -- -n -i /usr/local':
  cmd.run:

# install ycp
'curl https://s3.mds.yandex.net/mcdev/ycp/install.sh | bash -s -- -i /usr/local -n':
  cmd.run:
