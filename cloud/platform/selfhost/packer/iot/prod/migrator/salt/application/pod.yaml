apiVersion: v1
kind: Pod
metadata:
  clusterName: migrator
  name: migrator
spec:
  containers:
    - name: migrator
      image: registry.yandex.net/cloud/devices-migrator:{{ migrator_application_version }}
      command: ["sleep", "infinity"]
  hostNetwork: true
