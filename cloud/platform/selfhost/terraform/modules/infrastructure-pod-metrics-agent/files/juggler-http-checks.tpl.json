%{ for juggler_check_name in juggler_check_names ~}
    ,{
      "args": [
        "--service",
        ${jsonencode(juggler_check_name)}
      ],
      "check_name": "platform-http-check.py",
      "check_script": "platform-http-check.py",
      "format": "nagios",
      "interval": 30,
      "run_always": false,
      "services": [
        ${jsonencode(juggler_check_name)}
      ],
      "timeout": 30
    }
%{ endfor ~}
