# Update:
#  $ ycp --profile=gpn platform alb load-balancer update -r alb-lb.yaml
#

# Fields used for creation:
#folder_id: "bn3v8a9sj6u8ih3a088r"
#internal: true

load_balancer_id: fke1f2qjo6s8upu89g8k

name: "staging"
description: "Staging"
listener_specs:
- name: frontend-tls
  port: 443
  internal_address_spec:
    address: "::"
    ip_version: IPV6
  tls:
    default_handler:
      http_handler:
        http_router_id: fke8jcoh6f86tm0slii4
        is_edge: false
      tls:
        certificate_ids: ["cert-cpl"]
