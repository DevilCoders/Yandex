# Update:
#
#  $ ycp --profile=prod load-balancer network-load-balancer update b7r1i7ou1vves3g8vsn6 -r l3-nlb-spec.yaml
#

# Fields used for creation (also remove "address"):
#folder_id: b1gvgqhc57450av0d77p
#region_id: ru-central1
#type: EXTERNAL

update_mask: {paths: [attached_target_groups]} # Extra safety.

name: jaeger
labels: {}
listener_specs:
- name: "tls"
  port: 443
  target_port: 3443
  protocol: TCP
  external_address_spec:
    address: 2a0d:d6c0:0:1a::70
    ip_version: IPV6
    yandex_only: true
attached_target_groups:
- target_group_id: b7rmel3ep7lluvc7tj95
  health_checks:
  - name: "envoy-ping"
    timeout: 1s
    interval: 2s
    unhealthy_threshold: 2
    healthy_threshold: 2
    http_options:
      port: 30080
      path: /ready/no_drain
