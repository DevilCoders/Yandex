---
- name:          "juggler_check : {{ host }}-{{ service }}"
  juggler_check: ''
  vars:
  args:          "{{ default_infra_check  }}"
  with_items:
  - service:     "{{ service }}"
    host:        "{{ host }}"
    tags:        "{{ tags }}"
