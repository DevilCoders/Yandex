- name: 'juggler_check : envoy-healthcheck'
  juggler_check: ''
  args: "{{ default_check | hash_merge(item, timed_limits_32perc_crit_unreach_force_ok) }}"
  with_items:
    - service: envoy-healthcheck
      tags: "{{ tags }}"
